"""Reference implementations kept OUT of the training path.

Nothing under `reference/` is imported by `src/`, `main.py` or any campaign
config, and nothing here is frozen into a campaign manifest. It exists so a
rival method can be read and run without becoming part of the pipeline.
"""
