% tab_ablation_complete.tex -- complete component ablation (\input-ready float).
% Built from extra_experiments/ablation_complete.csv: OctMNIST hard-binding cells,
% n=24 paired per component (2 caps x 3 backbones x 4 seeds).
% Each row toggles one component off; Delta cc-F1 = full TraLO - (that component removed).
% Requires: booktabs (loaded by main.tex).
\begin{table}[t]\centering\small
\caption{\textbf{Complete component ablation on the OctMNIST hard-binding cells.} Effect on constrained-class F1 of removing each TraLO component in turn ($n{=}24$ paired runs each: 2 tight caps $\times$ 3 backbones $\times$ 4 seeds). $\Delta$~cc-F1 = full TraLO $-$ (component removed); winrate = fraction of the 24 paired runs where removing the component hurts. Inference is at the cell level (the unit that does not repeat seeds): removing either load-bearing component hurts in all six cells (sign test $p{=}0.031$, the six-cell floor), while the $\rho$ schedule and lambda freeze split the cells and are cc-F1-neutral on these already-satisfied cells. Two essential ingredients plus two neutral ones, \emph{not} four co-equal components.}
\label{tab:ablation_complete}
\begin{tabular}{l c c}
\toprule
Component removed & $\Delta$ cc-F1 & Winrate \\
\midrule
\multicolumn{3}{l}{\emph{Load-bearing}} \\
Optimizer reset   & $+0.079$ & $1.00$ \\
Undershoot hinge  & $+0.036$ & $1.00$ \\
\midrule
\multicolumn{3}{l}{\emph{Neutral}} \\
$\rho$ schedule   & $+0.001$ & $0.42$ \\
Lambda freeze     & $+0.000$ & $0.38$ \\
\bottomrule
\end{tabular}
\end{table}
