# README_DATA.md -- superseded

**Read `PROVENANCE.md`, in this directory, instead.**

This file described the corpus pipeline as it stood before the 2026-08
consolidation. It disagreed with `PROVENANCE.md` on whether `build_corpus.py`
exists (it does not, anywhere in the repository) and on which tables are
hand-maintained (three: `tab_ablation_complete`, `tab_deploy`,
`tab_oct_backbone`), while also calling itself obsolete and then restating the
obsolete claim two sections later.

Two provenance documents that disagree are worse than one, and this is the one
whose name makes a reader open it first. Its content is replaced by this
pointer rather than deleted, so the path keeps resolving for anything that
links to it.

`PROVENANCE.md` is the wiring diagram: what feeds what, which hops still have a
script, and what the corpus can no longer be rebuilt from.
