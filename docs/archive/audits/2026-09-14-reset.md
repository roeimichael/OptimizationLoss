# Evidence reset and validation receipt — 2026-09-14

> **ARCHIVED -- NOT CURRENT EVIDENCE.** This file is historical. It does not
> establish a result, a winner, a rejection, or a protocol, and it may be
> contaminated with retired datasets (dermMNIST leaked, BCN blocked, iwildcam
> retired). The evidence was reset on 2026-09-14. Do not cite, quote or reason
> from it. The live record is `RULESET.md`, `docs/MISSION.md`, `docs/LEDGER.md`
> and `docs/FRAMEWORK.md`. See `docs/archive/README.md`.


## Scope and status

User requested recoverable thinning, retirement of historical acceptance claims,
code/data/logging validation, then a within-TraLO change and monitored experiments.
This receipt records the initial reset, not completion of that whole program.
No new campaign was launched and no historical result was rescored as fresh evidence.

### Follow-up inspection: remaining cleanup (SSH snapshot 11:24 Asia/Jerusalem)

At `62581d90`, the local tree/index were clean and the GitHub branch SHA matched.
The app's +117,078/-33,372 matches the committed `origin/main...HEAD` comparison
exactly. No branch reset or history rewrite is needed to address uncommitted work.

Tracked physical Python lines: `src/` 7,440; `scripts/` 32,087;
`configs/` 2,342; `tests/` 23,956; `main.py` 192; historical/paper Python under
`docs/` 2,843. Separately, eight split-metadata CSVs contain 77,660 physical lines;
these are not model code. Counts include comments/blank lines and establish size,
not which files are necessary.

The generator still declares 42 arms. The reference TraLO block still emits
snapshot, count-mode, penalty-shape, item-scaling, projection, head-only,
straight-through and RNG-reseed switches. Optional readers and behavioral
branches remain in the runtime. Its constraint-phase defaults are FP32 off and
gradient clipping, not the current reference recipe. The historical rejection
ledger also remains embedded in the YAML. Thus Git artifact cleanup must NOT be
reported as completed model/config consolidation.

Local `results/` contains zero entries. The seven remaining server roots
`optloss-vitu`, `optloss-loosevit`, `optloss-iwc4`, `optloss-loose`,
`optloss-vitdom2`, `optloss-audit` and `optloss-select` have no files or links in
their `results/` directories. The validation checkout and primary server clone
have no `results/` directory. The 17 archived result trees remain external.
Unrelated projects and earlier explicitly named error archives were not changed.

This physical separation prevents ordinary active-root enumeration from finding
the moved campaigns. It is NOT a complete fresh-evidence policy: callers can
still explicitly supply historical paths, and `configs/task_windows.yml` carries
old measured windows used by generation and reporting. The next cleanup must
remove that automatic historical dependency and validate explicit fresh input
identity before any new comparison. No historical predictions were scored here.

Both hosts were reachable at 11:24. dsisco01: four Quadro RTX 6000 devices
(23,040 MiB each); dsisco02: four RTX PRO 6000 Blackwell Server Edition devices
(97,887 MiB each). All reported 0% GPU utilization and 0 MiB used, no compute
applications, and no account-owned Python/torchrun processes. Neither `squeue`
nor `qstat` was found on PATH; this does not establish that no institutional
reservation system exists. Availability must be rechecked before dispatch.

## Recoverable archival

| Collection | Preserved location | Verification |
|---|---|---|
| Local root `archive/` | `C:/Users/roeym/Desktop/projects/OptimizationLoss-history-20260914/local-archive` | 34,471 files/links, 2,742,861,255 bytes; 34,120 SHA-256 hashes; all inventory entries match after move |
| 17 server result trees | `/home/dsi/michaer8/optloss-history-20260914/<original-worktree>/results` | 21,954 files/links, 40,343,920,766 bytes; 18,180 SHA-256 hashes; all before/after entries match |
| Former operational narratives | `docs/archive/reset_2026-09-14/` | FRAMEWORK, MISSION, CLAUDE, AGENTS and old keepworking skill retained with archive banners |
| Pre-reset dirty source | `.codex/pre-reset-20260914.patch` and `.codex/pre-reset-docs-20260914.tar` | Local preservation receipts; patch does not include untracked files |

Hash coverage: CSV, JSON/JSONL, YAML, text, logs, Markdown, Python and shell files.
Other files, including checkpoints, were checked by relative path, size, mode,
timestamp and inode identity across the same-filesystem directory move, not by
content digest. Symlink targets were retained and checked. Nothing was unlinked.

Server source roots: `optloss-bcn`, `optloss-cutwin`, `optloss-dom`, `optloss-domb`,
`optloss-dualprop`, `optloss-equaldose`, `optloss-fmow`, `optloss-itemscale`,
`optloss-newunits`, `optloss-price`, `optloss-queue`, `optloss-score`, `optloss-shape`,
`optloss-snap`, `optloss-snap3`, `optloss-taskwin`, `optloss-uniform`.
The preserved trees contain 3,774 config files; that count does not mean validated
or independent runs. No folder-name quality classification was applied.

Each server tree has a `<original-worktree>-before.json` manifest beside it in
the new history root. Local manifest: `OptimizationLoss-history-20260914/local-archive-before.json`.
The one-off inventory helper remains at `.codex/reset_archive.py` locally and
`/home/dsi/michaer8/reset_archive_20260914.py` remotely.

The four `EXTENDS.json` files name sibling parents, not absolute paths. Moving
whole `results/` directories preserved those parent relationships. Dataset arrays,
Git worktrees/objects, earlier stale-data archives and paper source were not moved.
Neither SSH host had a training process when the result-tree moves began.

To restore: first confirm both hosts have no job using the target; require the
original path to be absent; move the archived directory back to its recorded
original path; rerun the inventory helper with the same receipt and `--verify`.
Do not overwrite new results to restore old ones. No symlinks from old result
roots were added, so active result globbing cannot silently include the archive.

Git packaging was subsequently cleaned in the user's separate task. Large
instruction copies and generated receipts stay on disk but are not all tracked;
`docs/GIT_TRACKING.md` records the verified ZIP, manifest and bundle recovery.
Do not infer deletion from their one-time removal from Git's index.

## Thinning and test retirement

The local archive alone removes **275 Python files / 37,287 physical Python lines**
from active workspace search without deleting them. The four large operational
documents formerly totalled 27,380 lines; compact current instructions replace
them and retain the original narratives in the history archive.

Retired `scripts/campaign_state.py`: it inferred state from fixed prose anchors
and historical tables. Its archived entry point refuses direct execution.
Retired prose-only tests are preserved in `retired-prose-tests.md`; mixed tests
retain CLI generation, quarantine/refusal, valid-reference and compute checks.
Executable loss, allocator, data-loader, metric and failure-path tests remain.
The date/test-count ratchets were removed rather than recreating stale paragraphs.

The static dead-code scanner still finds references to the remaining definitions.
This is not proof that every remaining probe is necessary. Further source/probe
consolidation requires checking consumers and behavior; it is not complete.

## Fresh dataset checks

Raw receipt: [dataset-arrays-20260914.json](dataset-arrays-20260914.json).
Arrays were memory-mapped on dsisco02; every resized image was SHA-256 hashed.
All three have aligned array lengths and metadata labels, non-null groups,
disjoint train/test group labels and disjoint filenames.

| Dataset | Train / test rows | Within-train / within-test duplicate rows | Exact image hashes crossing splits |
|---|---:|---:|---:|
| iwildcam | 20,000 / 2,943 | 5 / 0 | 0 |
| BCN | 8,270 / 3,900 | 19 / 11 | **2** |
| fMoW | 15,386 / 4,168 | 7 / 1 | 0 |

BCN cross-split pairs, zero-based rows:

| Train image / row / class | Test image / row / class |
|---|---|
| ISIC_0071017.jpg / 7319 / 1 | ISIC_0069013.jpg / 3053 / 0 |
| ISIC_0067502.jpg / 5846 / 1 | ISIC_0067980.jpg / 2836 / 0 |

The [official ISIC 2019 metadata](https://challenge.isic-archive.com/data/#2019)
was independently downloaded (SHA-256
`d93994a8ed201d474de9a7af7e17ec30929cbc4a5220659ecbde17cbe83e9316`).
All exported BCN filenames have a lesion ID and the splits share zero recorded
lesion IDs. The training pair maps to `BCN_0003560`; the test pair to `BCN_0000237`.
Thus lesion-ID disjointness did not catch these identical stored pixels.
Source-image corruption versus source duplicate/annotation inconsistency is not
yet determined. Do not silently discard either side or launch BCN as validated.
The follow-up pixel check rules out constant/blank arrays: the two distinct
images span values 1–201 and 40–218, with pixel standard deviations 42.52 and
49.57. This does not identify whether the source or preprocessing caused it.

fMoW also has zero cross-split site IDs under the filename-derived site rule.
All six local metadata CSVs match the remote content after CRLF-to-LF normalization;
BCN/fMoW are not byte-identical locally because of line endings. New release source
parity must compare the actual transferred bytes, not silently normalize them.
Exact pixel checks do not detect near-duplicates, every subject relationship,
or historical evaluation reuse. Dataset difficulty at development cuts is not
yet remeasured. No deduction about TraLO superiority follows from this audit.

## Correctness and instrumentation

- A real CPU GradScaler regression reproduces a masked-overflow path that returns
  `applied=True` although the scaler skipped the update. The shared step now reads
  scaler backoff after its update. This concerns masked AMP; it does not establish
  a fault in the default FP32 path.
- The alpha-liveness fixture uses raw probability differences and the same
  `2 * float32 epsilon` tolerance for positive/negative controls. A rounded hash
  crossing a bin is not evidence of a material response, and output sensitivity
  is not a mathematical equivalence proof.
- Optional shared-step diagnostics measure transformed gradient norm, actual
  parameter displacement and descent alignment. A zero-learning-rate fixture
  distinguishes calling a step from moving parameters. A matched Adam fixture
  verifies identical weights and RNG state with/without diagnostic collection.
- TraLO writes `constraint_events.jsonl`, schema 1, with attempt IDs, config digest,
  explicit count timing, per-scope counts/budget/residual/multiplier before/after,
  rho before/after, absolute and constraint-relative epochs, task/constraint dose,
  and applied-step diagnostics. Even the early-stopping epoch gets an event.
  Counts are the penalty inputs; estimator mode/straight-through flags are named.
  Existing CSV schemas remain readable. Rival-dual event integration, GPU cost,
  release/data identity fields and real-backbone validation remain pending.
- Independent review found two instrumentation defects and both were reproduced
  before fixing them: recovered AMP overflow aborted strict JSON serialization,
  and masking hid overflow from the diagnostic flag. Events now preserve
  nonfinite values as null plus explicit JSON-pointer/value markers, and the
  scaler-backoff flag survives masking. Nine focused tests pass; review cleared
  this scope. These are supported recovery paths, not reasons to hide an overflow
  or declare the campaign healthy despite repeated skipped updates.
- A fresh independent CPU probe found the optional `uniform` estimator computed
  its per-class mean slope separately per memory chunk. Default `sum` passed.
  The trainer now accumulates the clamped `p(1-p)` sum over all samples in pass 1,
  divides once by N, and reuses that detached weight for every backward chunk.
  Two new tests first reproduced the missing population-weight path, then passed;
  independent review checked uneven final chunks and saturated probabilities.
  This repairs memory-knob dependence, not empirical quality. Old uniform runs
  are not retroactively relabeled as runs of the repaired implementation.

The revised `keepworking` skill passed its structural validator and a clean-context
forward test. The old workflow correctly held launch in the baseline scenario,
but carried stale numeric rules and omitted multiplier/state timing fields. The
new reference addresses those omissions; no claim of improved launch decisions
is made from that single scenario.

Verification commands/receipts: focused constraint-event tests; full-suite XML
under `.codex/`; `scripts.audit_config`, `scripts.doc_commands`, `scripts.dead_code`.
Full-suite final status and server-release status belong in MISSION after those
checks finish. Passing tests does not certify universal correctness or novelty.

Target-environment checks found another rounded-hash trap in the old ALM liveness
test: its local prediction difference was only 5.96e-8, while the server's rounded
hashes matched. Direct gradients showed active augmentation in both environments.
The replacement uses two groups with analytically known residuals 1 and 3;
augmentation-only yields the expected raw gradient, switching it off yields no
step, and a nonzero-multiplier control changes normalized direction by L2 0.02315.
The same checks pass on PyTorch 2.5.1 locally and 2.11.0 remotely. Equality of
augmentation-off ALM and LDF is asserted only in this positive-residual fixture;
their signed versus positive-part dual updates differ after slack.

Final validation-snapshot receipts (not a campaign result):

- Local `abb18d27`: 668 passed, 1 skipped in 362.98 seconds;
  `.codex/reset-committed-20260914.xml`.
- Server, same runtime source with repaired ALM/table test files: 668 passed,
  1 skipped in 195.37 seconds; `.codex/reset-server-repaired-20260914.xml`.
  CUDA was hidden explicitly. Both skips are the absent real-campaign-log gate.
- Server test hashes: baseline fidelity
  `783188ac100dcbe3a7107980c6d29d2276df321a271867906fcfc2db96a608c8`;
  pipeline `3f529018f50d132d1f77fbd0740d69c4c11199069bb68105d1f89062fd88a007`.
  Later source-only packaging guards are being validated by the Git-hygiene task.
- Initial source export: all 424 tracked files matched actual local bytes;
  manifest SHA-256 `66b56d4b5aaa0454e1246bbbd0f8c8291651af72c271ef428b9d950ee049d74a`.
  Do not claim that old manifest describes later test/packaging edits. Re-sync
  the final clean commit and freeze a new manifest before any experiment.

## BCN source follow-up: duplicate origin resolved

The two known cross-split pairs are byte-identical JPEGs in the publicly served
[ISIC 2019 training archive](https://isic-archive.s3.amazonaws.com/challenges/2019/ISIC_2019_Training_Input.zip),
not just coincidentally equal 224x224 arrays. Read-only HTTP byte-range requests
verified each member's name, size, CRC and SHA-256 against the server cache;
all four images reproduce their stored resized arrays exactly. This rules out
our resize/export step as the origin of these particular duplicates.

The publicly downloaded ground-truth and metadata CSVs match the cached files
byte-for-byte. Each duplicate pair has MEL versus NV labels and different lesion
IDs/demographic metadata. This is an upstream dataset inconsistency; it does not
tell us which annotation is correct. No labels, images or split members changed.
BCN remains on hold pending an explicit, versioned curation policy and a renewed
whole-split audit; removing only whichever side helps a score is not acceptable.

Receipt: local ignored `docs/audits/2026-09-14-bcn-public-source.json`, including
all four member hashes, public URLs, ETag and selected metadata. Reproducer:
`.superpowers/sdd/lean-cleanup-plan/bcn_source_probe.py --verify-public`, executed
over SSH stdin on dsisco02 with Python3.10.19/numpy2.2.6/Pillow12.0.0. No GPU or
remote file write was used. The full 9.77GB ZIP was not rehashed or redownloaded;
verification is scoped to these four members and both full annotation CSVs.

## iWildCam annotation reconciliation

All 20,000 training and 2,943 evaluation metadata rows resolve uniquely to their
cached iWildCam2020 source annotation, with matching camera IDs and one consistent
source category per stored class. Training/evaluation use 143/7 source cameras
with no overlap. The source's 217,959 images have one annotation each, so this
slice does not exercise the preparation dictionary's ambiguous-last-label case.
Receipt: ignored `2026-09-14-iwildcam-source-lineage.json`, including the annotation
SHA-256 and eight-class ID/name mapping; reproducer `iwildcam_lineage_probe.py`
in the ignored cleanup ledger directory. This is cached-source reconciliation,
not independent label adjudication, image-mirror verification or proof that the
evaluation split was never inspected.

## fMoW crop identity: blocking preparation bug

Source metadata reconciliation found 7,429 repeated basenames across distinct
AOI directories in the cached 63,422-sidecar archive. The current slice contains
788 training and 301 test rows with multiple source crops per basename. Their site
class/country metadata agree, and full source-country/site sets remain disjoint
(136/13 countries, 3,362/795 sites). That does **not** establish crop-label alignment.
The mirror defines the full path without its extension as the sample ID and
describes AOI-specific crops, not basenames as unique images.
[Source format](https://huggingface.co/datasets/jbourcier/fmow-rgb-baseline/blob/main/README.md).

An independent pixel pass over the full cached image archive matched every
stored row exactly to its first basename occurrence: 15,386 train / 4,168 test,
zero missing images and zero multiple pixel matches. **436 train and 146 test
rows are `false_detection` AOI crops carrying the surrounding site's class.**
Example: test row 123 is labelled `single-unit_residential`, but its bytes match
`val/single-unit_residential/single-unit_residential_315/false_detection_160979/single-unit_residential_315_1_rgb.jpg`;
the actual residential crop under AOI 150057 has different pixels. The preparer
discards the AOI directory in load(), then collect() uses a basename dictionary
and pops the first tar member. This is a reproducible crop/label preparation
error, distinct from the upstream BCN annotation conflicts.

fMoW is on hold. Preserve the current arrays and metadata; repair full sample-ID
selection, reject ambiguous/missing members, and rebuild into a separately
versioned directory with explicit country/class continuity and renewed whole-
slice pixel/label/split checks. Do not patch labels based on model outcomes.
No data was modified and no GPU was used for this read-only investigation.

Receipts: ignored `2026-09-14-fmow-source-lineage.json` and
`2026-09-14-fmow-source-pixels.json`; reproducible stdin probes
`fmow_lineage_probe.py`/`fmow_pixels_probe.py` in the ignored cleanup ledger.
Full cached images archive SHA-256:
`6c8aaad054d60779a2aaf1faeedde39eae33d86598f0a5db3ac8eae416f7881b`;
metadata archive SHA-256:
`3089d4b0350ac6ae109c457a056d4050f91a4acd224ca9278a8ce67e2ea4519e`.
These checks reconcile to the cached mirror, not to an independently downloaded
original satellite dataset or an untouched evaluation holdout.
