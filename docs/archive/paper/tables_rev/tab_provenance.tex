% tab_provenance.tex -- which campaign each arm's runs came from, and whether it
% shares the frozen grid's cached warm-ups. Counts from data/manifest/experiments.csv
% (paper_final 1944, b1 312, b3+b3_full+b3_mnv2 408, b8 32, octmnist_MobileNetV2_* 120,
% review_graft 144, r2_seeds10 252). Warm-up sharing is a property of the cache key
% (src/config_generators/generate_configs.py::compute_base_model_id), which hashes
% backbone, dataset, seed and warm-up settings and does NOT include the methodology.
\begin{table}[t]\centering\small\color{blue}
\setlength{\tabcolsep}{4pt}
\captionsetup{labelfont={color=blue}}\caption{\color{blue}\textbf{Where each arm's runs come from.}
Cross-campaign drift is larger than the band we adjudicate at --- a same-seed run
whose warm-up is retrained lands $0.027$ from the frozen grid's stored value
(Sec.~\ref{sec:setup}) --- so every comparison in this paper is made
\emph{within} one campaign, and this table says which. The warm-up cache key
hashes backbone, dataset, seed and warm-up settings and does \emph{not} include
the method, so any run generated by cloning a frozen-grid configuration reuses
that configuration's cached warm-up rather than retraining one. The one arm
without a within-campaign control is the hinge-alone ablation row, and
Table~\ref{tab:ablation_complete} says so.}
\label{tab:provenance}
\begin{tabular}{l l r l}
\toprule
Arm & Campaign & Runs & Warm-ups \\
\midrule
Heur., LP-LG, Fioretto-LDF,      & \emph{paper\_final} & \multirow{2}{*}{$1944$} & \multirow{2}{*}{own} \\
\;Hounie-RCL, TraLO, TraLO-b     & (frozen grid)       &                         & \\
\addlinespace[1.5pt]
ALM, full grid                   & clones of \emph{paper\_final} & $324$ & shared \\
ALM, other cells                 & clones of their own hosts & $84$ & shared \\
\addlinespace[1.5pt]
MobileNetV2, Derm/Tissue         & \emph{paper\_backbones} & $288$ & own \\
MobileNetV2, OctMNIST            & four per-seed sweeps & $120$ & own \\
MobileNetV2, tight caps          & \emph{b8} & $32$ & own \\
\addlinespace[1.5pt]
Focal, class-balanced, logit-adj.& \emph{b1} & $312$ & own \\
Graft (+ re-run controls)        & \emph{review\_graft} & $144$ & own \\
Seeds $5$--$10$ replication      & clones of \emph{paper\_final} & $252$ & shared \\
\bottomrule
\end{tabular}
\end{table}
