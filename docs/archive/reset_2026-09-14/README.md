# Pre-reset material

The large AGENTS, CLAUDE, FRAMEWORK and MISSION snapshots remain at their
original local paths but are not distributed through GitHub. They are preserved
in the verified `OptimizationLoss-git-backup-20260914` evidence package and the
pre-cleanup Git bundle. See [recovery instructions](../../GIT_TRACKING.md).

The smaller archived code and audit notes here remain tracked. None of these
files overrides the current `docs/FRAMEWORK.md` or `docs/MISSION.md`.
