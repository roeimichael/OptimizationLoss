% ============================================================================
%  Two Portable Components for Meeting Hard Prediction Quotas
%  in Deep Classifiers
%
%  TMLR submission (anonymous). Single self-contained document (body + appendix).
%  Compile:  pdflatex main ; bibtex main ; pdflatex main ; pdflatex main
%  Figures live in figures/ ; tables are \input{} from tables/ ; bib = references.bib.
% ============================================================================
\documentclass[10pt]{article}
\usepackage{tmlr}
% For camera-ready:  \usepackage[accepted]{tmlr}
% For arXiv/preprint (deanonymized): \usepackage[preprint]{tmlr}

% We do NOT input math_commands.tex; our notation is light and self-contained.

\usepackage{hyperref}
\usepackage{url}
\usepackage{graphicx}
\usepackage{natbib}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{float}
% Use algpseudocode (modern replacement for algorithmic). Unlike algorithmic,
% algpseudocode does not define \AND, so there is no conflict with tmlr.sty's
% \AND used for author separation.
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{caption}
\usepackage{xcolor} % [Track-B revision] marks added content in blue
\usepackage[normalem]{ulem} % [Track-B revision] \del strikes superseded text in red
% Revision marks: \rev{...} adds in blue, \del{...} strikes in red.
% Fragile arguments (\citep, \ref) must be wrapped in \mbox inside \del.
\newcommand{\del}[1]{{\color{red}\sout{#1}}}

% [Track-B revision] Added content is set in blue: \rev{...} for inline text;
% added tables/blocks set \color{blue} locally. Delete this macro and the
% \color{blue} markers to strip all revision marks.
\newcommand{\rev}[1]{{\color{blue}#1}}

\graphicspath{{figures/}}

% ---------------------------------------------------------------------------
% [Formatting only] Float-page layout.
% LaTeX's default float-page glue (\@fptop = \@fpbot = 0pt plus 1fil) centres a
% full-page float vertically. tmlr.sty does not override it, so the tall
% per-cell tables were stranded with a large dead band above AND below. Pin
% float pages to the top and let all the slack collect at the bottom instead.
\makeatletter
\setlength{\@fptop}{0pt}
\setlength{\@fpsep}{12pt plus 1fil}
\setlength{\@fpbot}{0pt plus 1fil}
\makeatother
% Let a tall table share a page with text rather than claiming a page of its
% own. floatpagefraction must stay below topfraction (tmlr.sty sets 0.95).
\renewcommand{\floatpagefraction}{0.60}
\renewcommand{\textfraction}{0.07}
\renewcommand{\bottomfraction}{0.50}

% ----------------------------------------------------------------------------
\title{Two Portable Components for Meeting\\
Hard Prediction Quotas in Deep Classifiers}

% TMLR requires anonymous authors for submission; the tmlr style hides them.
\author{\name Anonymous \email anon@anon.edu \\
      \addr Anonymous Institution}

\begin{document}
\maketitle

% ----------------------------------------------------------------------------
\begin{abstract}
Deployed classifiers often face a hard cap on how many items of a known
evaluation batch may be assigned to a constrained class. Examples include
screening pools that can escalate only so many cases, sometimes with
per-group limits. We call this a transductive count constraint. We study
a training-time approach that satisfies this cap without editing
predictions after training, and we identify which of its components
drive the effect. The approach, which we call TraLO, combines a
saturating penalty, a ratcheted-and-frozen multiplier, an Adam-state
reset at first feasibility, and an undershoot hinge. Across a $1944$-run
grid on MedMNIST classification tasks ($28\times 28$ images upsampled to
$224\times 224$), constraint-time training reaches the cap natively in
nearly all runs, improving overall classification quality by $1.6$ to
$5.3$ percentage points over satisfying the cap after training. On
quality of the constrained class itself, one regime shows a consistent
advantage across all backbones tested: OctMNIST at tight caps, with the
largest gains on a ViT (not itself a rare-class regime). The rest of
the grid is a statistical tie, reported as such. Our central
mechanistic finding is that this constrained-class advantage is carried
by two portable components, the optimizer reset and the undershoot
hinge. Both transfer as additions to alternative constraint-trained
baselines and close most of the tight-cap gap. In contrast, the
saturating penalty shape after which the package is named proves neutral
by both ablation and graft. \del{Native-resolution generalization is left to
future work.}
{\color{blue}At native resolution, on one dataset run to full depth, the
quality margin over clipping reproduces in every backbone-by-cap cell.}
We release the full regime map, including the tie region
and the negative penalty-shape result, along with the per-run corpus.
\end{abstract}

% ============================================================================
\section{Introduction}
\label{sec:intro}

A surprising number of classification systems are deployed under a constraint
not on which items get a label but on \emph{how many} do. A screening program
reads a fixed pool of scans and may escalate only so many of them to
specialist review; an audit team can act on only so many flagged filings in a
quarter. In each case the operational requirement is a
\emph{hard upper bound on the count} of predictions in a constrained class over
the evaluation set. The items to be labeled are known in advance (a fixed
batch to be scored, not a stream), so the constraint is naturally
\emph{transductive}: it is a property of the whole test-set prediction vector,
not of any single example.

The standard way to honor such a cap is \emph{post-hoc clipping}: train a normal
model, rank the test items by their score for the constrained class, keep the
top-$K$, and reassign the rest. This is trivial and always meets the count, so
it is the realistic deployment baseline. But it meets the count by editing
predictions, often dozens of them, each a forced error on an item the model was
confident about, so accuracy degrades. The other end of the spectrum is
\emph{constrained training}: encode the cap as a constraint and solve the
Lagrangian with dual ascent
\citep{fioretto2020lagrangian,hounie2023resilient,chamon2020probably}. In
principle this is exactly the right formulation, but for a hard count the dual
multiplier of a linear penalty climbs without bound: on a one-epoch-warmup
probe (App.~\ref{app:mech}) it climbs steeply while a bounded multiplier
barely moves, for the same final count. The implementations remain competitive
and we do not over-read the escalation; it is a symptom, not the damage. What
it marks is a formulation that on some cells never reaches feasibility at all
(Sec.~\ref{sec:conv}).

\paragraph{This paper.} We characterize a third answer that lands on the count
by itself, without prediction surgery, and \emph{we identify which of its
ingredients actually do the work}. The package we study --- for exposition we
refer to it as TraLO (\emph{Transductive Loss Optimization}) --- combines a
saturating transductive penalty (a rational-saturation term plus a bounded
quadratic in the soft excess over the cap) with a multiplier that only moves
upward while the count is violated and freezes the first time it is met, an
Adam-state reset at first satisfaction, and an undershoot hinge that restores
constrained-class recall once the cap is met. The package as a whole meets the
count natively on essentially every run and beats post-hoc clipping on macro-F1
in nearly every regime. But the internal decomposition matters more than the
brand: our ablation and graft show that the constrained-class win rests on
\emph{two} of these ingredients --- the optimizer reset and the undershoot
hinge --- and that they transfer to the dual baselines, while the saturating
penalty shape after which the package is named proves neutral.

\paragraph{Who should care.}
\emph{Practitioners} building screening, audit, or triage pipelines under
hard batch caps on MedMNIST-scale tasks: this paper reports when
constraint-time training gains $1.6$--$5.3$ percentage points of overall
quality over satisfying the cap after training, at a compute cost of a few
extra minutes per run. \del{Whether the same margin holds at native imaging
resolution is not addressed here.}
{\color{blue}The same margin holds at native imaging resolution on the one
dataset we tested there \mbox{(Sec.~\ref{sec:limits})}.}
\emph{Researchers on constrained deep learning}: this paper isolates two
portable ingredients (an Adam-state reset at first feasibility and an
undershoot hinge) that transfer across constraint-trained methods and,
under matched training budgets, close most of the tight-cap gap between
them; the shape of the enclosing penalty proves neutral by both ablation
and graft.

\paragraph{Contributions.}
\begin{itemize}\itemsep2pt
  \item \textbf{An empirical characterization of when constrained training
  wins.} We formalize the operational ``$\le K$ predictions of class $c$''
  requirement as a transductive count constraint (Sec.~\ref{sec:method}) and
  map, over a $1944$-run grid, the regimes where constraint-trained methods
  beat clipping on macro-F1 (nearly all of the grid;
  Sec.~\ref{sec:deploy}) and where they beat one another on
  constrained-class F1 (essentially: OctMNIST tight caps;
  Secs.~\ref{sec:oct}--\ref{sec:regime}).
  \item \textbf{Two portable components that carry the constrained-class advantage.}
  An ablation isolates an Adam-state reset at first feasibility and an
  undershoot hinge (App.~\ref{app:ablation}) as load-bearing;
  a cross-method graft (Sec.~\ref{sec:carries}) shows they transfer to
  Fioretto-LDF \citep{fioretto2020lagrangian} and Hounie-RCL
  \citep{hounie2023resilient} and close most of the tight-cap gap. Neither
  component is derived; both are motivated empirically and identified by
  ablation.
  \item \textbf{The saturating penalty shape is quality-neutral.} The
  saturating penalty after which the package we study is named is neutral
  to constrained-class quality: swapping it for a linear penalty at the
  same ratcheted multiplier moves cc-F1 by $-0.001$ across $24$ runs,
  with $6$ runs landing on identical predictions
  (Sec.~\ref{sec:carries}; App.~\ref{app:shape}). Eq.~\eqref{eq:loss}
  earns its place by keeping the frozen multiplier small and readable,
  not by earning the quality gain.
  \item \textbf{A public regime map and per-run corpus.} We release the
  full $1944$-run grid (per-cell, per-seed) and the code, so any of the
  headline claims --- including the negative and tie regions --- is
  independently verifiable at the granularity we report them.
\end{itemize}

% ============================================================================
\section{Related Work}
\label{sec:related}

\paragraph{Lagrangian and dual constrained learning.}
Encoding requirements as constraints solved with primal--dual methods is
the classical recipe \citep{bertsekas1982constrained}. The idea was
introduced to neural networks by \citet{platt1988constrained} and brought
to modern deep networks by Lagrangian-duality training
\citep{fioretto2020lagrangian}, with PAC statistical footing
\citep{chamon2020probably,chamon2022constrained} and an end-to-end survey
in \citet{kotary2021end}. Resilient Constrained Learning
\citep{hounie2023resilient} adapts the constraint level to make hard
requirements attainable, and constraints can beat fixed penalties on
sparsity targets \citep{gallego2022controlled}. On the multiplier update,
PID-style rules stabilize the multiplier in safe RL
\citep{stooke2020responsive} and, more recently, in supervised deep
learning as PI controllers on the Lagrange multiplier
\citep{sohrabi2024pi}. Near-optimal solutions of constrained learning
problems have been characterized for non-convex losses
\citep{elenter2024near}, and open-source infrastructure for constrained
deep learning is now available \citep{gallego2024cooper}. Our critique of
runaway escalation is thus specific to the unbounded linear penalty:
augmented-Lagrangian, PI-controlled, and resilient updates are all better
behaved, yet the resilient method we benchmark still winds up slowly on a
hard count (Sec.~\ref{sec:conv}). \del{We do not run a dedicated
augmented-Lagrangian baseline. Its quadratic penalty growth is
structurally close to the $\rho$ ramp of the package we study (both bound
the constraint-term gradient after a first violation), so the two are not
independent tests of the same fix.}
{\color{blue}We do run a dedicated augmented-Lagrangian baseline
\mbox{(Sec.~\ref{sec:results})}. Its quadratic penalty growth is structurally
close to the $\rho$ ramp of the package we study, so it is not an
independent test of the same fix; we report it because it is the textbook
answer to linear-penalty windup, and a stronger dual is the sharper
comparison.}
The resilient baseline instead
represents the modern-controllers family with adaptive constraint
tightness. Our two dual-ascent baselines are Fioretto-LDF
\citep{fioretto2020lagrangian} and Hounie-RCL \citep{hounie2023resilient};
the package we study optimizes a bounded penalty and freezes the
multiplier at satisfaction. The reset and hinge components we identify
(Sec.~\ref{sec:method}) act on the optimizer state and the constraint
direction, not on the multiplier update itself. They are therefore
complementary to PI-controlled or resilient dual methods rather than
competing (see the graft experiment in Sec.~\ref{sec:carries}).

\paragraph{Output-level and fairness constraints.}
Closer to our output-level setting, prediction constraints have been enforced
during training by primal--dual updates \citep{nandwani2019primal} and by
linear label-count constraints for weakly supervised segmentation
\citep{pathak2015constrained}, or guaranteed per prediction by
input-dependent parametrizations \citep{brosowsky2021sample} or an
output-layer satisfiability solver \citep{goyal2024deepsade}.
\citet{marquez2017imposing} find hard enforcement feasible but no better than
soft penalties, motivating our bounded penalty. Constrained optimization has
protected under-represented classes \citep{sangalli2021constrained}, and
group-wise constraints underpin fair classification
\citep{bilal2015fairness,agarwal2018reductions,kearns2018preventing,donini2018empirical},
which our local caps resemble structurally, though a local cap is a
\emph{ceiling}, not a fairness floor (Sec.~\ref{sec:setup}).

\paragraph{Rate-constrained learning.}
The closest classical relatives constrain prediction \emph{rates}:
coverage as a dataset constraint \citep{goh2016satisfying}, non-decomposable
rate objectives through surrogate bounds \citep{eban2017scalable},
non-differentiable rate constraints through proxy-Lagrangian games
\citep{cotter2019optimization}, and generalized rate metrics through
multi-player schemes \citep{narasimhan2019optimizing}. These bound
\emph{expected} rates over a distribution, inductively; ours is a hard count
on one specific test set, verified on the argmax predictions and paired with
per-group local caps.

\paragraph{Post-hoc correction and resource allocation.}
Resource-constrained classification allocates a fixed budget over a trained
classifier's outputs at decision time, classically as a small assignment
problem, most recently under exactly our local-plus-global structure
\citep{shifman2025classification}. Related lines fold the budget into learning via adaptive cost-sensitive
weights \citep{shifman2023adaptive} or predict-and-optimize ranking
under stochastic capacity \citep{vanderschueren2024new}.
Learning-with-rejection \citep{cortes2016learning} instead prices
abstention per instance. All steer expected costs rather than a hard
count. Decision-time allocation always
meets the budget by editing predictions; our post-hoc baselines are a greedy
threshold heuristic and a linear-programming clipper (LP-LG), following
the local-and-global allocation formulation of
\citet{shifman2025classification}.

\paragraph{Cost-sensitive and imbalanced training.}
Reweighting or reshaping the loss changes which examples are emphasized:
cost-sensitive thresholds \citep{elkan2001foundations}, focal loss
\citep{lin2017focal}, class-balanced weights \citep{cui2019class},
label-distribution-aware margins \citep{cao2019learning}, prior-corrected
logits \citep{menon2020long}. None of these guarantees the prediction count, which the
constraint-trained methods studied here target directly. \del{Whether a
well-tuned imbalanced-training recipe followed by clipping would close
the macro-F1 gap over vanilla clipping is a question our current
experiments do not settle \mbox{(Sec.~\ref{sec:limits})}.}
{\color{blue}Whether a well-tuned imbalanced-training recipe followed by
clipping closes the macro-F1 gap is answered in Sec.~\ref{sec:deploy}: it
does on the imbalance-friendly dataset and not elsewhere, so the advantage
is dataset-dependent rather than uniform.}

\paragraph{Transductive and set-level constraints.}
Transductive inference reasons about a fixed unlabeled test set
\citep{vapnik1998statistical,joachims1999transductive}, where soft set-level
targets have long existed: expectation regularization pushes predicted class
proportions toward a prior \citep{mann2007simple}, and posterior
regularization constrains posterior expectations \citep{ganchev2009posterior}.
Our constraint is transductive in this sense but \emph{hard}: an explicit
argmax count budget, with per-group caps, enforced during training.

\paragraph{Few methods target both scopes at once.}
The families surveyed above address either a single global constraint
(dual-ascent methods, rate-constrained learning, expectation
regularization) or per-group constraints framed as fairness floors (fair
classification), but rarely both a global count cap and per-group local
caps that must hold at the same time on argmax predictions over a
specific test set. Under exactly this local-plus-global structure, the
closest recent work is the post-hoc allocator of
\citet{shifman2025classification}. On the constraint-trained side we
know of no baseline that handles both scopes jointly, so our benchmark
implementations of Fioretto-LDF and Hounie-RCL required adaptation to
enforce a global count together with per-group ceilings on argmax
counts (Sec.~\ref{sec:setup}).

% ============================================================================
\section{Problem and Method}
\label{sec:method}

\paragraph{Problem.}
Let the (known) test set contain $N$ items and let $p_\theta(y=c\mid x_i)$ be
the model's probability for the constrained class $c$ on item $i$. A
\emph{global} count constraint requires the number of items predicted $c$ to
satisfy $\sum_{i=1}^{N}\mathbf{1}[\hat y_i = c] \le K$. We parameterize the
cap as a percentage $L$ of the constrained class's true test-set count, so
$L$ measures how much of the class's actual demand the quota admits ($L30$
admits at most $30\%$ of the true count). \emph{Local} caps also bound the
count within each group $g$: $\sum_{i\in g}\mathbf{1}[\hat y_i=c]\le K_g$
(the same percentage of the group's own class count). Because the indicator
is not differentiable, training uses the \emph{soft count}
$S=\sum_i p_\theta(y=c\mid x_i)$, while satisfaction is verified with the
\emph{hard count} from $\arg\max$.

\paragraph{Overview of the package.}
The package we study --- for exposition we refer to it as \emph{TraLO}
(Transductive Loss Optimization) --- combines four ingredients: a saturating
per-constraint penalty (below); a multiplier that ratchets while the count is
violated and freezes the moment it is met; an Adam-state reset at first
satisfaction; and an undershoot hinge that engages once the count falls below
the cap. Not all four ingredients carry equal weight. Sec.~\ref{sec:carries}
and App.~\ref{app:ablation}--\ref{app:shape} show that the constrained-class
advantage over the strongest dual-ascent baselines rests specifically on the
reset and the hinge, that both transfer as portable additions to the dual
baselines, and that the penalty shape after which the package is named is
neutral by both ablation and graft. We describe the four ingredients below
before returning to the empirical story.

\paragraph{The constraint penalty.}
For a soft excess $E = \max(0,\, S - K)$ over a cap $K$, the per-constraint
penalty is
\begin{equation}
\label{eq:loss}
\ell(E;K,\rho)\;=\;\underbrace{\frac{E}{E+K}}_{\text{rational saturation}}
\;+\;\rho\,\underbrace{\frac{(E/K)^2}{1+(E/K)^2}}_{\text{bounded quadratic}} ,
\end{equation}
where both terms saturate as $E\!\to\!\infty$ (the first to $1$, the second
to $\rho$). What actually drives training is the penalty's \emph{gradient}
on the count; differentiating Eq.~\eqref{eq:loss} through $E=\max(0,S-K)$
gives, for $S>K$,
\begin{equation}
\label{eq:grad}
\frac{\partial \ell}{\partial S}
= \frac{K}{(E+K)^2} + \frac{2\rho}{K}\,\frac{E/K}{\bigl(1+(E/K)^2\bigr)^2}.
\end{equation}
Both the penalty and this gradient are bounded (App.~\ref{app:mech}). The
total objective combines cross-entropy with, per cap, the overshoot penalty
and an undershoot hinge $h$,
\begin{equation}
\label{eq:total}
L_{\text{total}} = L_{\text{CE}} + \lambda_g\,(\ell_g + h_g) + \lambda_l\,(\ell_l + h_l) ,
\end{equation}
where $\ell_g=\ell(E;K,\rho)$ is the global-cap penalty and
$h_g=\beta\max(0,\,K-S)/K$ its hinge (weight $\beta{=}0.5$, discussed below):
identically zero while $S\ge K$, hence inert throughout the violated phase,
it engages only once the soft count falls below the cap, in practice after
first satisfaction. $\ell_l$ and $h_l$ sum the same two terms over the
group caps; every cap (the global one and each group cap) carries its own
multiplier, so $\lambda_l\,(\ell_l+h_l)$ abbreviates
$\sum_g \lambda_{l,g}\bigl(\ell(E_g;K_g,\rho)+\beta\max(0,K_g-S_g)/K_g\bigr)$.

A cap's penalty depends on the parameters only through its scalar soft count
$S_c$, so its gradient always points along the same fixed direction
$\nabla_\theta S_c$: the multiplier, $\rho$, and the penalty's shape scale
that push but cannot redirect it, and under gradient clipping even the scale
cancels \citep{pascanu2013difficulty}. This is a partial argument only:
$\lambda$ multiplies only the constraint term and thus shifts the
cross-entropy-versus-constraint weighting in the combined update, so the
multiplier scale is not provably inert; and the shape reshapes $\ell'(S_c)$
at each $E$ rather than rescaling it, so it falls outside the scalar-rescaling
argument entirely. A matched probe with $\lambda$ differing ${\sim}280\times$
trains almost identically (App.~\ref{app:mech}), so the near-neutrality is
empirical rather than theoretical. So the saturating penalty of
Eq.~\eqref{eq:loss} sets the \emph{scale} of the constraint push, not the
update it produces: its shape is neutral by ablation and graft
(Sec.~\ref{sec:carries}), and the quality comes instead from Adam's reset
and the undershoot hinge, which change the direction.

\paragraph{Multiplier ratchet and freeze.}
Rather than dual ascent, $\lambda$ is \emph{ratcheted}: it increases by a
fixed step each epoch \emph{only while the hard count violates the cap}, and
is \emph{frozen} the moment the count is first satisfied. The bounded
penalty means the search ends at a small frozen multiplier rather than an
escalating one; multipliers start at $0.05$. The quadratic weight $\rho$
ramps linearly (from $5$ toward $100$) until first satisfaction and then
also freezes. Because $\lambda$ and $\rho$ both stop moving at first
satisfaction, the training procedure is really two-phase: while the count
is violated it \emph{searches} for a sufficient multiplier by ratcheting
$\lambda$ (the bounded gradient of Eq.~\eqref{eq:grad} keeps that search
ending at a small $\lambda$), and once the count is met it \emph{settles}.

\paragraph{Stability, and why a verification step is retained.}
Freezing $\lambda$ and $\rho$ at first satisfaction turns the moving-target
dual problem into a single fixed objective
$F(\theta)=L_{\text{CE}}+\lambda_g(\ell_g{+}h_g)+\lambda_l(\ell_l{+}h_l)$:
the tail of training descends one objective instead of chasing a multiplier
that is still moving. Because the penalty has a nonzero slope ($1/K$) at the
cap, a finite frozen multiplier suffices to pin the soft count there rather
than merely discourage violations, which is why the package reaches the cap
natively once cross-entropy saturates (App.~\ref{app:mech}). Feasibility is
defined by the \emph{hard} count while the penalty acts on the \emph{soft}
count, so a verification step (App.~\ref{app:deploy}) closes that small
residual gap.

\paragraph{Optimizer reset at satisfaction.}
At the first epoch the count is met, we reset the Adam optimizer state. This
removes the accumulated descent momentum that would otherwise keep pushing
the soft count far below the cap (over-satisfying at the cost of
constrained-class recall). The reset lets the frozen-multiplier objective
settle near the boundary instead of overshooting inward. It is motivated
empirically, not derived, and other reset variants (partial reset of only
first or only second moments, or decay-based) are not covered by our
experiments; the ablation (App.~\ref{app:ablation}) isolates the effect of
the full reset used here. The reset is applied only at the
constraint-satisfaction phase transition, and is distinct from continuous
learning-rate restart schedules \citep{loshchilov2017sgdr}, which are
orthogonal to the multiplier ratchet.

\paragraph{Undershoot hinge.}
A satisfied cap can still leave the model under-predicting the constrained
class, which costs recall. The hinge term $h$ in Eq.~\eqref{eq:total}
counters this: each cap $c$ contributes
$\lambda_c\,\beta\,\max(0,\,K_c-S_c)/K_c$, zero whenever the soft count sits
at or above its cap, so it can never re-violate; once the soft count drops
below the cap it gently pushes it back up toward (but not over) the cap,
restoring constrained-class recall. App.~\ref{app:ablation} shows the hinge
and the reset together carry the effect.

\paragraph{Training schedule.}
Training has a warmup phase (cross-entropy only, to a stable initialization
that is cached and reused) followed by the constraint phase under
Eq.~\eqref{eq:total}. A post-hoc verification step is available to enforce a
hard count if a run has not landed natively; for the reset+hinge package it
is essentially never needed (App.~\ref{app:deploy}). Algorithm~\ref{alg:tralo}
summarizes the loop.

\begin{algorithm}[t]
\caption{The training loop of the package studied here.}
\label{alg:tralo}
\begin{algorithmic}[1]
\State warm up $\theta$ on $L_{\text{CE}}$ alone; cache the warmup model
\For{each constraint epoch}
  \State update $\theta$ on $L_{\text{CE}} + \lambda_g\,\ell_g + \lambda_l\,\ell_l$ (plus the hinge once satisfied)
  \State recount hard predictions on the test inputs
  \If{a cap is violated}
    \State ratchet that cap's $\lambda$ up one step; advance $\rho$
  \ElsIf{first time all caps are met}
    \State freeze $\lambda$ and $\rho$; reset the Adam state
  \EndIf
  \State stop after $5$ consecutive satisfied epochs
\EndFor
\State post-hoc verification (flip borderline predictions) if needed
\end{algorithmic}
\end{algorithm}

% ============================================================================
\section{Experimental Setup}
\label{sec:setup}

\paragraph{Datasets.}
We use three MedMNIST v2 \citep{yang2023medmnist} classification tasks with
their official splits (Figure~\ref{fig:datasets} shows DermMNIST examples):
\textbf{TissueMNIST} (8 \rev{kidney-cortex-cell} classes \rev{from human kidney
tissue \citep{woloshuk2021situ}}, constrained class GE, $7.1\%$ of the
test set), \textbf{DermMNIST} (7 skin-lesion classes from
HAM10000 \citep{tschandl2018ham10000}, constrained class melanoma,
$11.1\%$), and \textbf{OctMNIST} (4 retinal-OCT classes \rev{from the
OCT-2017 dataset \citep{kermany2018identifying}}, constrained class
drusen, $25\%$ of a class-balanced test set). The first two carry the minority class that
the screening motivation assumes; OctMNIST does not, and we report it
because the cap binds hardest there. All three are $28{\times}28$ images
upsampled to $224{\times}224$ for the ImageNet-pretrained backbones. Local
caps use real groups only on DermMNIST (HAM10000 anatomical site:
torso/extremity/head-neck); TissueMNIST and OctMNIST lack group metadata and
use a fixed synthetic split, a mechanism test rather than a fairness
setting (App.~\ref{app:groups}). Caps are set as a percentage $L$ of the
constrained class's true test-set count (Sec.~\ref{sec:method}); the
asymmetric study decouples the two cap types, keeping $L$ for the per-group
caps and sweeping the global-cap percentage $G$ independently, so $G{<}L$
makes the global cap the binding one. That study runs all $20$ off-diagonal
$(L,G)$ pairs from $\{20,30,50,70,80\}\%$ on DermMNIST and TissueMNIST
(MobileNetV3, all methods, seeds $1$--$4$), re-running four shared $G{<}L$
pairs on RegNetY-400MF and ViT-B/16 (App.~\ref{app:regime}). The
percentage form only standardizes how hard the cap binds across datasets;
the method itself receives an exogenous integer $K$, exactly as a deployed
capacity would be given. All headline claims in this paper are scoped to
MedMNIST-derived tasks; extending to native-resolution medical or general
vision benchmarks is deferred to future work (Sec.~\ref{sec:limits})\rev{,
apart from a first native-resolution check there that ties the trained dual}.

\paragraph{Backbones.}
The headline grid uses three backbones, MobileNetV3
\citep{howard2019searching}, RegNetY-400MF \citep{radosavovic2020designing},
and a ViT-B/16 transformer \citep{dosovitskiy2020image}, to check that the
findings are not CNN-specific. All backbones use standard ImageNet-style
training conventions \citep{he2016deep}.

\paragraph{Baselines.}
Two \emph{post-hoc clippers}: a greedy threshold \textbf{Heuristic} and a
linear-programming clipper \textbf{LP-LG} (following the local-and-global
allocation formulation of \citealt{shifman2025classification}). Two
constraint-\emph{trained} dual-ascent
methods: \textbf{Fioretto-LDF} \citep{fioretto2020lagrangian} and
\textbf{Hounie-RCL} \citep{hounie2023resilient}. We also report
\textbf{TraLO-bounded}, an ablation of our own package (``bounded'' names
the bare saturating penalty it retains) that strips both load-bearing
additions, the optimizer reset and the undershoot hinge, used as a method
and as a within-cell control in App.~\ref{app:ablation}.

{\color{blue}
\paragraph{Imbalanced-learning baselines.}
Three \emph{imbalance-aware} training recipes, each trained with its own loss and then
LP-LG clipped, so the warm-up phase is their entire training budget
(Sec.~\ref{sec:deploy}). Let $p_y$ be the softmax probability of the true class, $n_c$
the training count of class $c$, and $\pi_c$ its prior. \textbf{Focal loss}
\citep{lin2017focal} replaces cross-entropy with
$-\alpha(1-p_y)^{\gamma}\log p_y$ ($\alpha{=}0.25$, $\gamma{=}2$); the modulating factor
$(1-p_y)^{\gamma}$ down-weights easy, high-confidence examples so the gradient
concentrates on hard and rare ones. \textbf{Class-balanced reweighting}
\citep{cui2019class} scales class $c$'s loss by the inverse \emph{effective} sample
count, $w_c = (1-\beta)/(1-\beta^{\,n_c})$ with $\beta{=}0.9999$, which discounts
information overlap among many similar samples and so raises the weight on rare classes.
\textbf{Logit adjustment} \citep{menon2020long} adds a prior-based offset to the logits,
$z_c \leftarrow z_c + \tau\log\pi_c$ with $\tau{=}1$, shifting the decision threshold to
compensate for label imbalance. None of the three enforces a count: they change
\emph{which} class is predicted, not \emph{how many}, so each still requires the post-hoc
LP to meet the cap.}

\paragraph{Baseline adaptations.}
The two trained baselines were built for a different setting, so we
re-instantiated each on the transductive count task and ran it inside the
same harness as our own package: the same cached warmup, $300$-epoch budget
and stop rule (five consecutive feasible epochs), gradient clipping, and
post-hoc verification step. Two details are specific to the port: each
method's constraint gradient targets the unlabeled test set, where the
count is enforced (the originals target a training or population
expectation), and each method's reported checkpoint is selected on the
constraint-excess axis (best feasible, else lowest excess), never on F1, so
no baseline is credited with its best-F1 epoch. \textbf{Fioretto-LDF} is
otherwise unchanged, with no analog of the sample-size rescaling applied to
Hounie-RCL below: a linear penalty $\lambda_c S_c$ with per-constraint
subgradient ascent
$\lambda_c\!\leftarrow\!\lambda_c+\eta\max(0,S_c\!-\!K_c)$ on the test
counts. \textbf{Hounie-RCL} keeps its primal/slack/dual updates, with the
constraint residual read as $(S_c\!-\!K_c)/N$. The one substantive fix in
our port is to normalize its primal count-gradient by the sample count so
the primal step and the dual update share a scale. Otherwise the primal
term is $N\times$ too strong and over-suppresses the capped class.
\textbf{LP-LG} is post-hoc only: a global-and-local linear program that
allocates the capped labels on the warmup model's test-set softmax, using
an identity misclassification cost rather than the general cost matrix
of the original two-phase allocator of
\citet{shifman2025classification}.

\paragraph{Frozen recipe.}
All methods share one protocol, fixed before the final runs:
ImageNet-pretrained backbones, $224{\times}224$ inputs, batch size $64$, no
augmentation, Adam with learning rate $10^{-4}$ during warmup and
$5{\times}10^{-6}$ in the constraint phase; warmup $=50$ epochs
(cross-entropy only), constraint phase $=300$ epochs, seeds $1$--$4$ (all
reported, no seed selection). Compute is modest: the constraint phase runs
a median $2$--$7$ minutes per run on one GPU for our package and the dual
baselines alike, on top of a shared cached warmup (clipping itself costs
seconds but pays the quality tax of Sec.~\ref{sec:deploy}). Per-method
constraint steps were fixed a priori, not tuned per cell: dual step $0.005$
(Fioretto-LDF), dual and relaxation steps $0.01$ (Hounie-RCL) on all three
datasets, and ratchet step $0.002$ with hinge weight $\beta{=}0.5$ (TraLO).
A step-fairness sweep (App.~\ref{app:mech}) varies the baseline steps
over a $10\times$ range, and a matching sweep varies our own $\beta$ and
$\lambda_{\text{step}}$ (App.~\ref{app:tralo-sweep}): the OctMNIST
tight-cap advantage is insensitive to both (the best baseline variant still
trails by $0.019$; TraLO clears the baseline at every arm). The headline
grid is \emph{paper\_final}: $3$ backbones $\times\,3$ datasets $\times\,9$
cap levels $\times\,6$ methods $\times\,4$ seeds $=1944$ runs. Models are
implemented in PyTorch with torchvision backbones and MedMNIST v2 loaders,
and each run trains on a single commodity NVIDIA GPU, with no multi-GPU or
distributed training. Code, the configuration generators, and the full
per-run results corpus accompany the submission as the Code and Data
Supplement.

\paragraph{Metrics.}
Our \emph{primary} metrics are quality: \textbf{constrained-class F1
(cc-F1)}, the quality of the capped class itself and the quantity the
constraint most directly puts at risk, and \textbf{macro-F1} for overall
quality. As \emph{secondary} deployment diagnostics we also report
\textbf{native satisfaction} (the fraction of runs whose final model meets
the global count with no post-hoc edit) and \textbf{flips} (the number of
test predictions the post-hoc verification step overwrites to enforce the
global and per-group caps; $0$ when the run needs no adjustment); we treat
these as deployment \emph{properties} rather than headline metrics, since
every deployable method meets the hard count one way or another
(App.~\ref{app:deploy}). Gaps versus baselines are \emph{paired} by seed:
because the seeds are matched, we summarize a method's advantage by its
seed-winrate and a paired Wilcoxon test rather than by marginal $\pm$std
bars, which would understate a paired effect when a baseline is
seed-unstable but its per-seed gap to TraLO is consistent. Claims spanning
several cells are tested on cell means (cells are the independent units;
the four seeds recur across cells); within one cell, $n{=}4$ comparisons
are reported as effect sizes and seed counts, since a four-pair Wilcoxon
cannot fall below $p{=}0.125$.

\paragraph{Family-wise multiple-comparisons control.}
With many cells and multiple axes of comparison, some fraction of significant
paired tests are expected under the global null. Rather than apply a blanket
Bonferroni correction --- very conservative under our paired design, since it
would penalize the same four seeds recurring across cells --- we report all
seed-winrates, effect sizes, and cell-level $p$-values, so that a
Benjamini-Hochberg step-up procedure at any target FDR can be recomputed from
the released per-run corpus. As a check, the OctMNIST tight-cap headline
(six cells, cell-level sign test $p\!=\!0.031$ per load-bearing component)
still survives a Benjamini-Hochberg step-up at $q\!=\!0.05$ across the twelve
cell-by-component comparisons behind Table~\ref{tab:ablation_complete}: the
six-cell floor $p\!=\!0.031$ maps to a BH threshold of $q\!=\!0.05\times
6/12 = 0.025$ for the smallest $p$-value and $q\!=\!0.05\times 12/12 = 0.05$
for the largest, and both load-bearing components lie under their respective
thresholds.

% ============================================================================
\section{Results}
\label{sec:results}

\subsection{Constrained-class quality vs.\ the trained baselines}
\label{sec:oct}

We start with constrained-class F1 (cc-F1), the quality of predictions on
the very class the cap restricts; Table~\ref{tab:ccf1} shows the full
symmetric grid, all three datasets over all three backbones, on both
headline metrics (cc-F1 here, macro-F1 in Sec.~\ref{sec:deploy}). The
sharpest result is where the count binds hard: at the OctMNIST tight caps
($L30$/$L40$, the shaded band of Figure~\ref{fig:octmnist}) the package
leads the best constraint-trained baseline on every backbone, by a mean
cc-F1 gap of $+0.038$ across the six cells (sign test $p{=}0.031$) and
peaking at $+0.081$ on the ViT-B/16 (per-backbone paired deltas in
Table~\ref{tab:oct_backbone}). Where the cap is slack the summary is a tie:
TraLO is best-or-tied on cc-F1 (within $0.005$) against the two trained
baselines in $23$ of the $27$ cells ($22$ when our own TraLO-bounded
ablation variant is also counted as a competitor), and on tissue and derm
the differences at equal feasibility are within seed noise (paired
Wilcoxon, not significant), a tie that also holds away from the grid, at a
tighter ($L40$) and a looser ($L60$) cap on RegNet.

\begin{figure}[!t]\centering
\includegraphics[width=0.7\linewidth]{fig_octmnist}
\caption{\textbf{The OctMNIST tight-cap advantage is backbone-general and
largest on the ViT-B/16.} \emph{Top:} constrained-class F1 of all six
methods (MobileNetV3, mean over $n{=}4$ seeds); the curves nearly coincide,
so the advantage is invisible at this scale (shaded band = tight-binding
regime; $^{\dagger}$ faded post-hoc clippers can post higher raw cc-F1 by
editing). \emph{Bottom:} TraLO minus the best constraint-trained baseline,
paired by seed, per backbone; the same inverted-U appears on all three,
largest on the ViT-B/16.}
\label{fig:octmnist}
\end{figure}

\input{tables/tab_ccf1}

\paragraph{The hard regime: OctMNIST tight caps.}
The one place the package pulls clearly ahead of the trained baselines is
OctMNIST at tight caps, where the cap binds hard but the drusen class is
still discriminable. There, TraLO leads the best trained baseline on cc-F1
at every backbone and every seed ($4/4$ per cell, $24$ of $24$ seed-level
paired comparisons across the six cells), with the margin largest on the
ViT-B/16; the six tight-cap deltas are in Table~\ref{tab:oct_backbone}.
Treating cells as the independent unit, the six cell-mean gaps are all
positive; the $t$-test on the same six cell means gives $p{=}0.013$, and
the same cells improve macro-F1 at five of the six (the sixth a $+0.001$
wash; mean $+0.012$). The advantage is recall at the cap, not precision:
TraLO matches the best trained baseline on constrained-class precision
($0.90$ vs $0.91$) and beats it on recall ($0.31$ vs $0.27$), the quantity
the undershoot hinge restores. The duals lose that recall from below:
penalized only above the cap, they overshoot it downward and finish
$11$--$36\%$ under the budget at $L30$ (cell-mean counts $48$--$67$ of
$K{=}75$, vs $73$--$74$ for TraLO), so recall falls with the unspent
budget while precision holds. A tight cap holds recall near its ceiling
$K/N$ at high precision, and training recovers the leftover headroom: a
property of the cap, not the $28{\times}28$ resolution. Figure~\ref{fig:octmnist}
plots the sweep: the six methods nearly coincide (top), while the
per-backbone gap (bottom) reveals the advantage and its inverted-U. Where a
post-hoc clipper posts higher raw cc-F1 (three of the six band cells, by
up to $+0.018$), the edge is bought by editing: at OctMNIST $L30$ on
MobileNetV3, clipping keeps the base model's $75$ most confident drusen
calls and rewrites the ${\sim}110$ evicted predictions ($11\%$ of the
batch) to each sample's runner-up class, so the bill lands off-class and
TraLO leads that same cell $0.702$ to $0.666$ on macro-F1
(App.~\ref{app:deploy}).

\paragraph{Robustness of the OctMNIST advantage.}
Three checks. (i) \emph{Backbone-general}: leave-one-backbone-out
cross-validation (define the winning band on the two training backbones,
test on the held-out one; script in the released corpus) holds on every
held-out backbone: pooled held-out gap $+0.033$ at a $93\%$ seed-winrate,
and $100\%$ on the tight caps themselves. (ii) \emph{Re-derivable without
the gap}: a rule that never sees the TraLO-vs-baseline gap flags a cell
when the cap binds (the clipper is forced into $\ge 65$ flips) and the
class is still discriminable (mid-range cc-F1: recall headroom left at
high precision). Every flagged cell is a TraLO advantage
(win-rate $100\%$), vs $0$--$40\%$ on unflagged cells ($0\%$ over-tight,
rising to $40\%$ at loose caps); the discriminability test reads test
labels, so this re-derives the regime for analysis, not a deployment-time
selector. (iii) \emph{Inverted-U, not monotone}: the effect is an
inverted-U in tightness, not ``tighter is always better,'' and it is
OctMNIST-specific. This is the one \emph{backbone-general} cc-F1
advantage; the full regime map follows (Sec.~\ref{sec:regime}).

{\color{blue}
\paragraph{Stronger dual, a fourth backbone, and interval estimates.}
Three further checks sharpen the tight-cap result. \emph{(iv)~A stronger dual does not
close the gap.} We add the augmented Lagrangian (ALM)
\citep{bertsekas1982constrained} --- the textbook fix for the
linear-penalty windup noted in Sec.~\ref{sec:intro} --- as a fourth
constraint-trained baseline. ALM is genuinely stronger than plain Fioretto-LDF (it
beats it by $+0.010$ cc-F1), yet TraLO still leads it on all six OctMNIST tight-cap
cells, by $+0.028$ cc-F1 on average (Table~\ref{tab:almbb}, top). \emph{(v)~A fourth
backbone.} The advantage reproduces on MobileNetV2 ($+0.047$/$+0.037$ cc-F1 at
$L30$/$L40$ over the best trained dual), so the backbone-general claim now spans four
architectures (Table~\ref{tab:almbb}, bottom). \emph{(vi)~Interval estimates.} A
cell-level bootstrap ($20{,}000$ resamples) puts the cc-F1 gap over Fioretto-LDF at
$+0.046$ ($95\%$ CI $[{+}0.024,{+}0.074]$) at $L30$ and $+0.033$
($[{+}0.021,{+}0.044]$) at $L40$, both excluding zero, as do the gaps over ALM
($[{+}0.010,{+}0.048]$ and $[{+}0.008,{+}0.046]$); under Benjamini--Hochberg control
($q{=}0.05$) three of the eight tight-cap cells --- the six of Sec.~\ref{sec:oct} plus the
two MobileNetV2 cells added here --- survive against Fioretto-LDF and none
against the post-hoc clipper. With $n{=}4$ seeds a within-cell paired test cannot fall
below $p{=}0.125$, so most cells cannot reach individual significance whatever the effect
size; the informative contrast is three versus none, and it is consistent with the cc-F1
tie against clipping noted above.}

\begin{table}[t]\centering\small\color{blue}
\setlength{\tabcolsep}{5pt}
% \color{blue} must be repeated INSIDE \caption: the caption package issues
% \normalcolor before typesetting caption text, which would otherwise reset it.
\captionsetup{labelfont={color=blue}}\caption{\color{blue}\textbf{The OctMNIST tight-cap win holds against the
stronger ALM dual and on a fourth backbone.} Paired gap of TraLO, mean over seeds
$1$--$4$. \emph{Top:} over the augmented-Lagrangian dual (ALM), which itself beats
plain Fioretto-LDF by $+0.010$ cc-F1; TraLO leads all six cells. \emph{Bottom:} on
MobileNetV2, over the best trained dual.}
\label{tab:almbb}
\begin{tabular}{lll rr}
\toprule
Comparison & Backbone & Cap & $\Delta$ cc-F1 & $\Delta$ macro-F1 \\
\midrule
\multirow{6}{*}{TraLO $-$ ALM}
 & MobileNetV3   & L30 & $+0.009$ & $-0.004$ \\
 &               & L40 & $+0.013$ & $+0.002$ \\
 & RegNetY-400MF & L30 & $+0.048$ & $+0.017$ \\
 &               & L40 & $+0.008$ & $+0.002$ \\
 & ViT-B/16      & L30 & $+0.046$ & $+0.021$ \\
 &               & L40 & $+0.046$ & $+0.020$ \\
\midrule
\multirow{2}{2.2cm}{TraLO $-$ best trained dual}
 & \textbf{MobileNetV2} & L30 & $+0.047$ & $+0.022$ \\
 &                      & L40 & $+0.037$ & $+0.016$ \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Wins, ties, and losses across the full grid}
\label{sec:regime}

Across the full corpus the observed pattern is: \textbf{TraLO tends to win
where the count binds hard \emph{and} the constrained class stays
discriminable, and to tie where either condition fails.} Binding alone is not enough: at
$L30$ the warmup model over-demands the cap by $2.4$--$4.8\times$ on tissue
and derm, more than OctMNIST's $2.0$--$2.5\times$, yet only OctMNIST clears
the win bar. We score the \emph{paper\_final} grid on the three backbones
plus dedicated MobileNetV3 asymmetric sweeps, partitioned into five
constraint-geometry regimes (tight, middle, and loose symmetric caps:
$L{=}G{\le}40$, ${=}50$, ${\ge}60$; asymmetric caps with the global side,
$G{<}L$, or the group side, $G{>}L$, tighter), every regime reported
(per-cell breakdown in App.~\ref{app:regime}). A regime counts as a
\emph{win} only when the mean paired cc-F1 gap and at least half its
cells clear $+0.005$, and a \emph{loss} under the mirror-image rule; no
regime mean falls below $-0.005$. The $+0.005$ effect-size threshold
was chosen from inspection of the ablation and graft magnitudes in
Sec.~\ref{sec:carries} rather than pre-registered; the OctMNIST
tight-cap regime survives generously wider thresholds (paired cell-mean
gaps range $+0.016$ to $+0.081$; Table~\ref{tab:oct_backbone}), so its
classification does not turn on the threshold choice, while the
DermMNIST asymmetric result sits closer to the threshold and is
reported as \emph{preliminary} for that reason as well as for its
MobileNetV3-only backbone coverage. Only two regimes clear the win bar:
OctMNIST tight-symmetric caps (the backbone-general advantage of
Sec.~\ref{sec:oct}, largest on ViT-B/16) and the global-tighter
asymmetric ($G{<}L$) caps of DermMNIST ($+0.8$pp over MobileNetV3's full
asymmetric grid, reproduced on RegNet and tied on ViT over the four shared
cap pairs; \emph{preliminary}, as only MobileNetV3 has the full grid).
Every other regime, including all symmetric caps, is a statistical tie
(symmetric tissue+derm mean cc-F1 gap $-0.0013$, seed winrate $29\%$,
$n{=}216$), and none is a loss. Against the post-hoc clippers macro-F1 is
higher for TraLO on every regime (Sec.~\ref{sec:deploy}). All of these
statements are scoped to the three MedMNIST-derived datasets we ran.

% ----------------------------------------------------------------------------
\subsection{Quality vs.\ post-hoc clipping}
\label{sec:deploy}

The other baseline family is the field default: train unconstrained, clip
the top-scoring predictions down to the cap. Both end up meeting the
count; the question is what quality that costs. Clipping meets the cap by
overwriting predictions (often dozens per run), an accuracy tax that the
constraint-trained methods --- landing on the count natively --- avoid.
Across the full $1944$-run grid (run-level, paired by seed, against the
best post-hoc clipper) this is a macro-F1 gain for TraLO of $+0.053$
(Tissue), $+0.016$ (Derm) and $+0.021$ (OctMNIST), positive in every
regime and, by cell-mean paired gap, in $77$ of the $81$ symmetric cells
(per cell at the displayed caps in the macro-F1 block of
Table~\ref{tab:ccf1}; App.~\ref{app:regime} has all $81$). The four
exceptions, two of them ties within $0.001$, sit at the very tightest
$L10$/$L20$ caps, where the cap pins every method's constrained-class
recall at the same ceiling, so the margin is decided off-class and the
clipper's frozen base model keeps its unconstrained accuracy there. The
gain comes with constrained-class F1 moving the same way in aggregate.
The margin over clipping is a property of training under the constraint
rather than of TraLO alone: the dual baselines post nearly identical
macro-F1 margins (the lone exception a tie at OctMNIST$\times$ViT), so we
claim it for the constraint-trained family; constraint-time training also
adapts to the test batch, a confound we do not separate from avoided
editing in the current experiments (Sec.~\ref{sec:limits}). What is
TraLO's own is reaching that quality with the fastest and most reliable
native convergence (Sec.~\ref{sec:conv}) and the fewest prediction edits
at binding caps. The tax is steepest where the cap binds: at the tight
$L30$ cap, clipping costs up to $0.061$ macro-F1 (Tissue;
App.~\ref{app:deploy}). How TraLO avoids the tax (near-perfect native
satisfaction, an order of magnitude fewer edits) is a deployment property
reported in App.~\ref{app:deploy}: what separates the methods is the
quality each preserves while meeting the count. In deployment terms:
train under the constraint when the full label vector ships, and clip only
when the capped shortlist is the sole deliverable, a shortlist assembled
by rewriting $5$--$11\%$ of the batch at binding caps.

{\color{blue}
\paragraph{Imbalanced-learning baselines.}
A natural challenge to the margin over clipping is whether an \emph{imbalance-aware}
clipper --- one trained to favour the rare constrained class before clipping ---
recovers the macro-F1 that plain clipping loses, matching the constraint-trained
family without ever training under the count. We add the three imbalance-aware recipes
defined in Sec.~\ref{sec:setup} --- focal loss \citep{lin2017focal}, class-balanced
reweighting \citep{cui2019class}, and logit adjustment \citep{menon2020long} --- each
trained with its own loss and then LP-LG clipped, at the tight $L30$ cap
(Table~\ref{tab:imbal}). Focal is the only competitive member, and across all three
backbones the pattern is \emph{dataset}-driven rather than backbone-driven. On
DermMNIST --- the imbalance-friendly dataset, where an imbalance-aware loss should do
well --- focal matches or slightly reverses TraLO's macro-F1 margin on every backbone
($-0.012$, $+0.006$, $-0.020$). On TissueMNIST TraLO leads on two of the three
($+0.020$, $+0.017$), and on OctMNIST it leads on MobileNetV3 ($+0.025$) and ties on
the other two. Class-balanced roughly ties and logit adjustment is weak throughout. The
macro-F1 advantage is thus dataset-dependent, reinforcing that it belongs to the
constraint-trained \emph{family} rather than to TraLO alone. What none of the four clip-based baselines provides is
native cap satisfaction: all require the post-hoc LP ($1.0$ vs $0.0$), which remains
TraLO's categorical separation from every clip-based method.}

\begin{table}[t]\centering\small\color{blue}
\setlength{\tabcolsep}{6pt}
\captionsetup{labelfont={color=blue}}\caption{\color{blue}\textbf{Imbalanced-learning baselines, macro-F1.}
Paired macro-F1 gap of TraLO over focal loss, class-balanced reweighting, and logit
adjustment (each trained with its own loss, then LP-LG clipped), at the
tight $L30$ cap (mean over seeds $1$--$4$; positive $=$ TraLO better).
All four arms come from one paired campaign; the comparison against the
plain clipper over the full grid is Table~\ref{tab:ccf1}. Focal is the
only competitive baseline, and the pattern is dataset-driven rather than
backbone-driven.}
\label{tab:imbal}
\begin{tabular}{ll rrr}
\toprule
Dataset & Backbone & vs.\ focal & vs.\ class-bal. & vs.\ logit-adj. \\
\midrule
DermMNIST   & MobileNetV3   & $-0.012$ & $-0.016$ & $+0.021$ \\
            & RegNetY-400MF & $+0.006$ & $+0.022$ & $+0.042$ \\
            & ViT-B/16      & $-0.020$ & $-0.017$ & $+0.025$ \\
\addlinespace[2pt]
OctMNIST    & MobileNetV3   & $+0.025$ & $+0.023$ & $+0.021$ \\
            & RegNetY-400MF & $-0.005$ & $+0.022$ & $-0.000$ \\
            & ViT-B/16      & $-0.004$ & $+0.010$ & $-0.006$ \\
\addlinespace[2pt]
TissueMNIST & MobileNetV3   & $+0.020$ & $+0.014$ & $+0.026$ \\
            & RegNetY-400MF & $-0.002$ & $+0.003$ & $-0.001$ \\
            & ViT-B/16      & $+0.017$ & $+0.025$ & $+0.010$ \\
\bottomrule
\end{tabular}
\end{table}

% ----------------------------------------------------------------------------
\subsection{Convergence}
\label{sec:conv}

These advantages rest on TraLO reaching the count during training.
Figure~\ref{fig:convergence} tracks how far each method's prediction count
sits above the cap over the constraint phase, on the cell where the three
methods separate most cleanly: DermMNIST on RegNet, whose cross-entropy
saturates slowly enough that the count and the task objective openly
compete. The ratcheted multiplier acts from the first constraint epoch and
reaches the cap well before either zero-start dual baseline (per-method
epochs in the caption). The gap holds grid-wide: native feasibility is
$100\%$ for TraLO and Hounie-RCL but only $87\%$ for Fioretto-LDF, whose
unbounded multiplier stalls short of the cap on the rest. Across
DermMNIST and TissueMNIST on the two convolutional backbones, over the
$18$ symmetric-cap cells we log, Hounie-RCL is the slowest of the three in
every one and TraLO is never the slowest: TraLO reaches the count first
or within one epoch in $15$ of the $18$ (strictly first in $11$), at
median epochs-to-feasibility of $17$, $20$, and $49.5$ for TraLO,
Fioretto-LDF, and Hounie-RCL. Fioretto-LDF is competitive when it converges
but stalls on the runs it does not, so TraLO is the only method that
reaches the count both fast and on every run, which lets it avoid the
editing that costs clipping its quality. The per-cell census accompanies
the released corpus.

\begin{figure}[t]\centering
\includegraphics[width=0.55\linewidth]{fig_convergence}
\caption{\textbf{TraLO reaches the count fast and holds it.} Best-so-far
constraint excess (lowest predictions over the cap so far) over the
constraint phase; DermMNIST (RegNetY-400MF), tight cap $L{=}G{=}30$.
Median over $n{=}4$ seeds; band: interquartile range; markers at each
method's median epoch-to-feasibility. All three start about $200$ over the
cap; on this cell TraLO's ratchet reaches the cap first, the zero-start
duals later. Giving the baselines TraLO's nonzero start narrows but does
not close this (App.~\ref{app:init}).}
\label{fig:convergence}
\end{figure}

% ============================================================================
\section{What Carries the Win: Ablation and Graft}
\label{sec:carries}

The previous section reports TraLO's advantages over the constraint-trained
and post-hoc-clipping baselines. This section asks the internal question:
\emph{which of the four ingredients in the package produces those
advantages, and are they specific to TraLO or transferable?} Two
converging experiments answer it: a within-cell ablation that removes each
ingredient from TraLO and re-runs the OctMNIST tight-cap cells
(App.~\ref{app:ablation}), and a cross-method \emph{graft} that adds the
same two ingredients to each dual baseline and re-runs the same cells
(App.~\ref{app:graft}). A third experiment (App.~\ref{app:shape})
substitutes a linear penalty for Eq.~\eqref{eq:loss} to isolate the
contribution of the penalty shape itself.

\paragraph{Ablation isolates the reset and the hinge.}
On the six OctMNIST tight-cap cells where TraLO leads, we disable one
component at a time and measure the paired change in constrained-class F1
($n{=}24$ per component; Table~\ref{tab:ablation_complete}). Two
components are load-bearing. Removing the Adam-state reset at satisfaction
costs $+0.079$ cc-F1, the single largest effect, helping on all $24$
paired runs and hence in all six cells (cell-level sign test $p\!=\!0.031$).
Removing the undershoot hinge (the TraLO-bounded control, already in the
main grid) costs $+0.036$ cc-F1 (positive on all $24$ paired runs;
cell-level sign test $p\!=\!0.031$; $0.019$--$0.070$ across cells). The
other components are cc-F1-neutral on these already-satisfied cells: the
$\rho$ schedule and the lambda freeze move it by under $0.001$ (n.s.).

\paragraph{Graft transfers the win to the dual baselines.}
The converse test adds those two ingredients to the dual baselines. We
graft exactly the optimizer reset and the undershoot hinge onto
Fioretto-LDF and Hounie-RCL (each using the host's own multipliers and
penalty) and re-run the six tight-cap cells: $144$ runs in all, including
re-run controls for TraLO and both hosts under identical warmup caches,
the frozen recipe, and one GPU model. The re-run controls reproduce the
main-grid values to $\pm0.0013$, so the arms are compared on equal
footing. The graft lifts Fioretto-LDF in six of six cells ($+0.019$ to
$+0.073$ cc-F1) and Hounie-RCL in five of six ($+0.035$ to $+0.103$; the
one exception, $L40$ on RegNet, drops by $0.028$), recovering a median
$86\%$ of TraLO's margin over the Fioretto host and $92\%$ over the
Hounie host (medians over the six cells, the loss cell included). Against
each grafted host separately TraLO stays best-or-tied ($8$ wins, $4$
ties, $0$ losses under the main-text win rule). Seed-by-seed against the
stronger of the two grafted hosts, though, TraLO's mean edge is only
$+0.0015$ and it trails in two of the six cells: \emph{once the recipe is
grafted the three methods are statistically interchangeable}, with what
remains of TraLO's lead concentrated on the ViT-B/16 ($+0.015$/$+0.017$
over the grafted Fioretto-LDF at the two caps). This is the paper's
central mechanistic finding: the two portable ingredients, not the host,
carry the constrained-class advantage.

\paragraph{Penalty shape is neutral.}
A third arm swaps Eq.~\eqref{eq:loss}'s saturating penalty for a linear
one at the same ratcheted multiplier, holding the ratchet, the freeze,
the reset, and the hinge fixed --- so the only change is the shape after
which the package is named. The arm is genuinely different (its logged
constraint loss runs about $3\times$ smaller throughout); its
predictions are not. cc-F1 moves by $-0.001$ (the bounded shape wins $9$
of $24$ runs and loses $9$, with $6$ exact ties; sign-test $p{=}1.0$)
and median time-to-feasibility by two epochs. In $6$ of the $24$ runs the
two arms land on \emph{identical} cc-F1: changing the penalty shape
changed no prediction at all, consistent with the fixed-direction
argument of Sec.~\ref{sec:method}. We report this as a negative result
about our own formulation: Eq.~\eqref{eq:loss} earns its place by keeping
the frozen multiplier small and readable, not by earning the
constrained-class gains, which belong to the reset and the hinge.

\paragraph{Anti-windup as a mechanistically-limited control.}
As a further control, Fioretto-LDF with dual restarts
\citep{gallego2022controlled} deploys predictions \emph{identical} to
plain Fioretto-LDF in every cell, because the deployed checkpoint is
selected on the constraint-excess axis before first feasibility and the
restart machinery only acts after feasibility. The control therefore
cannot in principle change the deployed model. What it does show is that
the linear-penalty comparison in the main grid is not a strawman created
by omission of a standard post-feasibility fix; it does not rule out
that a stronger anti-windup scheme acting before feasibility could close
the gap.

% ============================================================================
\section{Limitations}
\label{sec:limits}

We foreground the limitations because several of them are load-bearing for
how the reader should generalize.

\paragraph{Dataset scope.}
All three datasets are drawn from MedMNIST v2 and are $28{\times}28$
images upsampled to $224{\times}224$ for the ImageNet-pretrained
backbones. Every headline claim in this paper is scoped to that setting.
\del{We do not know how the reset and hinge transfer to native-resolution
medical imaging (say ISIC-2019 at full resolution), to non-medical}
{\color{blue}We take one step past that setting below, on native
$224{\times}224$ HAM10000, where the margin over clipping reproduces. We
still do not know how the reset and hinge transfer to other
native-resolution medical imaging (say ISIC-2019), to non-medical}
long-tailed benchmarks (ImageNet-LT), or to text/graph settings. The
regime map itself may re-derive at other scales --- the mechanistic story
(dual ascent overshoots downward, hinge restores recall) is not
resolution-specific --- but this is a hypothesis, not an evidenced claim.

{\color{blue}
\paragraph{A native-resolution check on HAM10000.}
We took one step past the upsampled setting, on a single dataset run to full depth:
native $224{\times}224$ DermMNIST (HAM10000), the standard warm-up-$50$ budget, every
method and every backbone in the paper --- $\{$TraLO, Fioretto-LDF, Hounie-RCL, LP-LG$\}
\times \{$MobileNetV3, RegNetY-400MF, ViT-B/16$\} \times \{L30, L40\} \times 4$ seeds,
$96$ runs (Table~\ref{tab:native}). Three things carry over. \emph{(i)~The quality margin
over clipping survives}: TraLO leads LP-LG on macro-F1 in \textbf{all six}
backbone$\times$cap cells ($+0.013$ on average), which is the deployment claim of
Sec.~\ref{sec:deploy} reproducing at native resolution. \emph{(ii)~Against the trained
duals it is a tie}, as the regime map predicts for a loose-to-moderate cap on a
discriminable class: grand-mean cc-F1 gaps of $-0.001$ (Fioretto-LDF) and $+0.001$
(Hounie-RCL), well inside seed noise. \emph{(iii)~Native satisfaction is unchanged}:
TraLO meets the count with no post-hoc edit in \textbf{all $24$ of its runs}, against
$0$ of $24$ for LP-LG, which needs the LP every time; Fioretto-LDF stalls short of the
cap on one seed at RegNetY-400MF$\times L40$ ($0.75$). What this does \emph{not} settle
is the win region: OctMNIST has no native-resolution counterpart (it exists only at
$28{\times}28$), so whether the tight-cap cc-F1 advantage itself survives at full
resolution remains open.}

\begin{table}[t]\centering\small\color{blue}
\setlength{\tabcolsep}{4.5pt}
\captionsetup{labelfont={color=blue}}\caption{\color{blue}\textbf{Native-resolution
HAM10000: the full method $\times$ backbone grid.} Native $224{\times}224$ DermMNIST
(HAM10000), constrained class melanoma, warm-up $50$, mean over seeds $1$--$4$;
\textbf{bold} $=$ within $0.005$ of the best in row within each metric block, i.e.\ the
paper's tie band, so jointly bolded entries are tied rather than ranked. Against the
trained duals (Fioretto-LDF, Hounie-RCL) every cell is a tie, apart from a single
$+0.008$ macro-F1 lead over Hounie-RCL (MobileNetV3, $L40$); against the LP-LG clipper
TraLO leads on macro-F1 in all six cells, and the clipper is the only method outside the
tie band in every row of that block. Native cap satisfaction over the $24$ runs per method: TraLO
$1.00$, Hounie-RCL $1.00$, Fioretto-LDF $0.96$, LP-LG $0.00$.}
\label{tab:native}
\begin{tabular}{ll cccc cccc}
\toprule
 & & \multicolumn{4}{c}{cc-F1 (melanoma)} & \multicolumn{4}{c}{macro-F1} \\
\cmidrule(lr){3-6}\cmidrule(lr){7-10}
Backbone & Cap & TraLO & Fioretto & Hounie & LP-LG & TraLO & Fioretto & Hounie & LP-LG \\
\midrule
\multirow{2}{*}{MobileNetV3} & L30 & \textbf{0.414} & \textbf{0.414} & \textbf{0.419} & \textbf{0.419} & \textbf{0.712} & \textbf{0.714} & \textbf{0.709} & 0.702 \\
                             & L40 & \textbf{0.500} & \textbf{0.498} & \textbf{0.497} & \textbf{0.497} & \textbf{0.735} & \textbf{0.732} & 0.727 & 0.717 \\
\addlinespace[2pt]
\multirow{2}{*}{RegNetY-400MF} & L30 & 0.410 & 0.409 & 0.405 & \textbf{0.417} & \textbf{0.716} & \textbf{0.717} & \textbf{0.715} & 0.703 \\
                             & L40 & \textbf{0.484} & \textbf{0.487} & \textbf{0.482} & 0.479 & \textbf{0.730} & \textbf{0.732} & \textbf{0.730} & 0.715 \\
\addlinespace[2pt]
\multirow{2}{*}{ViT-B/16}    & L30 & \textbf{0.381} & \textbf{0.378} & \textbf{0.381} & 0.365 & \textbf{0.679} & \textbf{0.681} & \textbf{0.679} & 0.670 \\
                             & L40 & 0.455 & \textbf{0.462} & 0.455 & 0.446 & \textbf{0.699} & \textbf{0.697} & \textbf{0.696} & 0.684 \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{The advantage over trained baselines is regime-specific.}
Only two regimes clear the win bar: OctMNIST tight symmetric caps (the
backbone-general result) and the DermMNIST global-tighter asymmetric caps
(preliminary, MobileNetV3-only for the full grid). Every other regime is
a statistical tie. A reader who cares only about tight-cap regimes on
discriminable classes will find the advantage useful; a reader in the
tie region has no reason to prefer TraLO to a grafted dual baseline on
constrained-class quality.

\paragraph{Motivation--result mismatch on OctMNIST.}
The screening motivation in Sec.~\ref{sec:intro} imagines rare, high-stakes
classes. OctMNIST's drusen class is neither: OctMNIST's test split is
class-balanced by construction ($250$ items per class), and drusen is a
relatively benign OCT finding compared with CNV or DME. Readers should
not take the OctMNIST result as evidence about rare-class clinical
screening; a clinical deployment would more plausibly cap the urgent
classes than drusen. The OctMNIST cells earn a place in this paper
because the cap binds hardest there and the class stays discriminable,
not because the story matches deployment.

\paragraph{Confound between constraint-time training and test-batch
adaptation.} The macro-F1 gain over post-hoc clipping is claimed for the
constraint-trained family in aggregate. But constraint-time training on
the test batch (transductive fitting) is a second thing our methods do
that vanilla clipping does not, and the current experiments do not
separate the two: some of the +$1.6$--$5.3$\,pp macro-F1 gain plausibly
comes from test-batch adaptation rather than from avoided editing per
se. An inductive variant (constraint on train-set count only) would
separate them; we did not run it.

% \leavevmode forces the run-in heading out before \del opens its colour group,
% otherwise the heading itself is typeset red.
\paragraph{\del{Missing baselines from imbalanced learning.}
{\color{blue}Imbalanced-learning baselines.}}\leavevmode
\del{We compare against constraint-trained dual ascent and post-hoc clipping,
but not against focal loss \mbox{\citep{lin2017focal}}, class-balanced weighting
\mbox{\citep{cui2019class}}, or logit adjustment \mbox{\citep{menon2020long}} followed
by clipping. If any of these closes the macro-F1 gap over vanilla
clipping, the ``constraint-trained family beats clipping'' claim
tightens to ``constraint-trained beats \emph{vanilla} clipping.''}
{\color{blue}We compare against focal loss \citep{lin2017focal},
class-balanced weighting \citep{cui2019class}, and logit adjustment
\citep{menon2020long} followed by clipping in Sec.~\ref{sec:deploy}. Focal
does close the gap on DermMNIST, so the ``constraint-trained family beats
clipping'' claim tightens on that dataset to ``constraint-trained beats
\emph{vanilla} clipping''; the count-satisfaction separation is unaffected,
since none of the three meets the cap without the post-hoc LP.}
\del{The comparison is a natural next experiment.}

\paragraph{The reset is ad hoc; other reset variants are not covered.}
The optimizer reset at first satisfaction is a full Adam-state reset
(first and second moment estimates both zeroed). Partial resets --- only
$m$, only $v$, decay-based rather than binary --- are plausible
alternatives; our experiments do not cover them. The evidence identifies
that \emph{some} reset at satisfaction is load-bearing, not that this
specific reset is optimal.

\paragraph{Small $n$ per cell.}
Each cell has $n{=}4$ seeds. Within-cell paired Wilcoxon tests cannot
fall below $p{=}0.125$, so we report within-cell effects as effect sizes
and seed counts, and reserve $p$-values for cell-mean statistics ($n{=}6$
cells at the tight-cap regime). Studies powered to detect small
per-cell effects would need more seeds.

\paragraph{Transductive-only scope.}
The formulation is transductive: it enforces a hard count on a known
evaluation batch, scored in one shot. Online/streaming settings where
items arrive one at a time and the count budget must be spent over time
are outside the scope of this work. A natural extension would replace
the ratcheted global multiplier with a running budget-consumption
controller and the transductive count with an expected residual on the
remaining stream, but we do not evaluate this here.

\paragraph{Naming caveat.}
The package studied here is named for its saturating penalty shape
(Eq.~\eqref{eq:loss}), yet the ablation and graft show the shape is
neutral to constrained-class quality. Readers looking to reuse the
findings should carry the reset and the hinge, not the specific penalty
of Eq.~\eqref{eq:loss}: it is a scale-setter, not a quality-setter.

% ============================================================================
\section{Discussion and Conclusion}
\label{sec:conclusion}

Count caps are a real, common deployment constraint whose two standard
answers each cost something: post-hoc clipping pays an accuracy tax for
editing predictions, and dual-ascent training chases the cap with an
escalating multiplier and, in its linear-penalty form, can stall short of
it. A third answer --- constrained training with a saturating penalty, a
multiplier frozen at satisfaction, an optimizer reset, and an undershoot
hinge --- matches the strong constraint-trained baselines across the grid
and beats them on the constrained class where the cap binds hard and the
class stays discriminable (OctMNIST tight caps; preliminarily,
DermMNIST's global-tighter asymmetric caps), and improves macro-F1 over
post-hoc clipping while landing on the count natively.

Two findings of this paper are worth stating separately from the
package. First, the constrained-class advantage over the strongest
dual-ascent baselines rests on \emph{two} specific ingredients --- an
Adam-state reset at first feasibility and an undershoot hinge --- that
transfer to the dual baselines and close most of the gap, while the
saturating penalty after which the package is named proves neutral by
both ablation and graft. Second, the regime map shows that hard-count
constrained training pays back over clipping essentially everywhere on
our grid, while beating the trained baselines only in a narrow,
identifiable regime; the map is what a practitioner needs, not the
brand of any one package. We release the full corpus so both findings
--- and every tie and negative result behind them --- are independently
verifiable at the granularity we report them.

% ============================================================================
\subsubsection*{Broader Impact Statement}
This paper studies methods that enforce hard prediction quotas in
classification systems. Such quotas often arise in screening, audit, or
triage pipelines where downstream capacity (specialist time, investigator
attention) is limited. There are two potential harms worth flagging.
First, a hard cap on flagging a class is a \emph{ceiling} on how many
items are escalated, not a floor on how well the population is served;
misapplied as a fairness intervention, per-group caps can lock in
under-diagnosis of groups where the cap is set from a biased prevalence
estimate. Section~\ref{sec:related} and App.~\ref{app:groups} distinguish
the ceiling reading from a fairness floor, but we cannot control how
downstream users set $K_g$. Second, we evaluate on MedMNIST-derived
tasks (28$\times$28 upsampled); the paper does not license clinical
deployment on the datasets we use, and readers should not read the
OctMNIST result as evidence about rare-class clinical screening
(Sec.~\ref{sec:limits}). Beyond these two, the constrained-training
recipe has no capabilities beyond ordinary supervised classification and
no new dual-use concerns that we can identify.

\subsubsection*{Reproducibility Statement}
All headline claims in this paper are backed by the released code and
per-run results corpus. Specifically: the training and evaluation code,
the configuration generators, and the raw per-cell/per-seed outputs for
all $1944$ runs of the headline grid (plus the ablation, graft,
anti-windup, penalty-shape, hyperparameter-sweep, initialization, and
cross-backbone controls behind
Secs.~\ref{sec:oct}--\ref{sec:carries} and Apps.~\ref{app:mech}--\ref{app:groups})
accompany this submission at
\url{https://anonymous.4open.science/r/tralo-anonymous}
(anonymized during review; will be de-anonymized on acceptance). All
methods share the same frozen recipe (Sec.~\ref{sec:setup}), fixed before
the final runs; per-method constraint step sizes were fixed a priori, not
tuned per cell, and are documented alongside the code. The four seeds
($1$--$4$) are all reported, with no seed selection. Cell-level statistics (paired Wilcoxon, sign tests,
seed-winrates) are recomputable from the released per-run corpus using
the analysis scripts included. Compute for the full grid is modest
(median $2$--$7$ minutes per constraint-phase run on one commodity GPU),
which we hope keeps external replication practical.

\bibliography{references}
\bibliographystyle{tmlr}

% ============================================================================
% APPENDICES (merged from the AAAI supplementary document, xr no longer needed
% because everything is in one PDF now).
% ============================================================================
\appendix

\section{Mechanism: Bounded vs.\ Unbounded Dual Behavior}
\label{app:mech}

This appendix supports the convergence and mechanism story of
Sec.~\ref{sec:conv} in two ways: first, it establishes analytically that
the saturating penalty and its gradient are bounded and that a finite
frozen multiplier suffices to pin the soft count at the cap; second, it
reports a one-epoch-warmup probe that isolates the constraint-descent
dynamics from the cross-entropy saturation of the paper grid, exposing
where dual ascent applies force it cannot use.

\paragraph{Bounded penalty and gradient.}
Write $u=E/K\ge 0$. In Eq.~\eqref{eq:loss} the first term $E/(E+K)=u/(1+u)$
increases from $0$ to $1$ and the second $\rho\,u^2/(1+u^2)$ increases from
$0$ to $\rho$, so $0\le\ell<1+\rho$. Differentiating through
$E=\max(0,S-K)$ gives Eq.~\eqref{eq:grad} for $S>K$. There the first
summand $K/(E+K)^2=\tfrac1K(1+u)^{-2}$ is at most $1/K$ (at $u{=}0$) and
decreasing; the second, $\tfrac{2\rho}{K}\,u/(1+u^2)^2$, is maximized at
$u=1/\sqrt3$ with value $\tfrac{9\rho}{8\sqrt3\,K}<\rho/K$ and tends to
$0$ as $u\to\infty$. Their sum therefore lies in $[0,(1+\rho)/K]$ and
vanishes as $E\to\infty$. Scaling $\ell$ and $\partial\ell/\partial S$ by
a frozen multiplier $\lambda$ gives the bounds $\lambda(1+\rho)$ and
$\lambda(1+\rho)/K$, neither depending on $E$.

\paragraph{Rank-one gradient direction.}
$E_c=\max(0,S_c-K_c)$ is a function of the scalar $S_c$, and $\ell$ is a
function of $E_c$, so $\lambda_c\ell(E_c)$ is the composition
$\theta\mapsto S_c(\theta)\mapsto\lambda_c\ell$. The chain rule gives
$\nabla_\theta[\lambda_c\ell]=\lambda_c\,\ell'(E_c)\,(\partial
E_c/\partial S_c)\,\nabla_\theta S_c=\lambda_c\,(\partial\ell/\partial
S_c)\,\nabla_\theta S_c$, using
$\partial E_c/\partial S_c=\mathbf 1[S_c>K_c]$. Set
$a_c=\lambda_c\,\partial\ell/\partial S_c\ge 0$. Summing over caps gives
a gradient in $\mathrm{span}\{\nabla_\theta S_c\}_c$, a subspace of
dimension at most the number of caps.

\paragraph{Gradient clipping absorbs the scalar.}
Let $g=\nabla_\theta L_{\text{CE}}+\sum_{c'}a_{c'}\nabla_\theta S_{c'}$. In
the limit where one cap's term dominates and the remaining terms vanish,
$g\to a_c\nabla_\theta S_c$ with $a_c>0$; norm-clipping, when active
($\lVert g\rVert>\tau$), then returns
$\tau\,g/\lVert g\rVert\to\tau\,\nabla_\theta S_c/\lVert\nabla_\theta
S_c\rVert$, in which the positive scalar $a_c$ (hence $\lambda_c$, $\rho$,
and the shape of $\ell$) cancels.

\paragraph{A finite multiplier suffices.}
Write $g(S)=\ell(\max(0,S{-}K);K,\rho)$. For $S<K$, $g\equiv0$, so $g$ has
left-derivative $0$ at $K$. For $S>K$,
$g'(S)=K/(E+K)^2+\tfrac{2\rho}{K}\,u/(1+u^2)^2$ with $E=S-K$ and $u=E/K$
(Eq.~\eqref{eq:grad}); as $S\to K^+$ the first term $\to1/K$ and the
second $\to0$, so $g$ has right-derivative $1/K$ at $K$, independent of
$\rho$. Parameterize a path along which the soft count rises through $K$,
and let $c=\lvert dL_{\text{CE}}/dS\rvert_{S=K}$ be the cross-entropy pull
at the cap; the constrained class is under-served at a cap $L<100\%$, so
$dL_{\text{CE}}/dS\le0$ there. The one-sided derivatives of
$F=L_{\text{CE}}+\lambda g$ at $K$ are $F'(K^-)=-c\le0$ and
$F'(K^+)=-c+\lambda/K$. If $\lambda\ge cK$ then $F'(K^+)\ge0$, so $F$
does not decrease past the cap: $S{=}K$ is a local minimum along the
path, and a finite multiplier attains the cap. The nonzero boundary slope
$1/K$ is what makes the penalty exact, playing the role of the $\ell_1$
penalty's unit slope \citep{nocedal2006numerical}.

\medskip
Figure~\ref{fig:loss_shape} plots these two bounds against the violation
size. Why does a hard count cap stress the dual-ascent baselines? On the
paper grid the interaction is hidden: after the $50$-epoch warmup the
classifier is already saturated, and a saturation gate shared by every
method (train accuracy $\ge 0.995$ on two consecutive checks) stops
cross-entropy updates within the first two constraint epochs, so the
constraint phase is pure count optimization for all methods alike. We
therefore probe with a one-epoch warmup, where the classifier keeps
learning throughout the constraint phase and the two objectives visibly
compete (Figure~\ref{fig:mechanism}).

The probe shows three things. First, while the classifier is learning,
the count cannot be forced down: each epoch's cross-entropy updates
re-inflate the constrained class, and the excess oscillates around its
starting level for ${\sim}60$ epochs under both methods
(Fig.~\ref{fig:mechanism}c). Dual ascent responds exactly as designed:
the violation persists, so the multiplier grows every epoch, from $1$ to
$53$ (seeds $2$--$3$: $58$, $60$). In control terms this is integrator
windup: the controller keeps integrating an error it has no ability to
act on. Second, the escalation buys nothing. Every method takes a
single gradient-clipped constraint step per epoch (max-norm $1.0$,
shared across methods), so extra multiplier cannot buy extra descent:
the two cross-entropy traces are numerically indistinguishable
(Fig.~\ref{fig:mechanism}b), and the count comes down no faster at
$\lambda{=}53$ than at TraLO's ratcheted $\lambda\le 0.19$. The
asymmetry is in what the clip rail is doing. At $\lambda{=}53$ an
unbounded-penalty gradient would dominate the update by orders of
magnitude were it not clipped, whereas TraLO's bounded penalty at
$\lambda\le 0.19$ demands orders of magnitude less force in the first
place. Third, the count falls only once the classifier saturates and the
gate stops CE updates (dotted lines in Fig.~\ref{fig:mechanism}c), and
then both methods reach the cap within a few epochs. The count outcome is
identical; what differs is the pressure applied to the constrained class
to get there.

The paper grid shows the same behavior in compressed form: the dual
multipliers escalate while TraLO's freezes ($\le 0.09$ there),
Fioretto's native satisfaction is ${\sim}0.87$ ($0.84$ on OctMNIST and
TissueMNIST, $0.93$ on DermMNIST), so its unbounded multiplier stalls
short of the cap on ${\sim}13\%$ of runs that never reach feasibility
within the budget (Sec.~\ref{sec:conv}); runs that end violated restore
their best-satisfied checkpoint and remain competitive.

Nor is the escalation a step-size artifact. A dedicated fairness sweep
(Block~C of the released corpus; $96$ runs on the hard-regime cells)
varies Fioretto's dual step over $0.001$--$0.01$ and Hounie's $\eta$
over $0.001$--$0.1$: Fioretto's constrained-class F1 moves by less than
$0.001$ across the $10\times$ step range, larger Hounie steps only hurt
($0.43\!\to\!0.35$), and no setting closes the gap to TraLO on the
OctMNIST tight cells (the best variant still trails by $0.019$ cc-F1).

We state this mechanism \emph{qualitatively only}. An audit shows that
the magnitude of the escalation does not predict where TraLO wins (the
multiplier is in fact largest in some cells where TraLO loses). So the
claim is ``dual ascent needs an unbounded multiplier plus a clip rail
to hold a hard count, while TraLO's bounded, frozen multiplier holds it
alone,'' not any dose--response of the form ``more escalation
$\Rightarrow$ bigger win.''

\begin{figure}[t]\centering
\includegraphics[width=0.7\linewidth]{fig_loss_shape}
\caption{\textbf{Bounded vs.\ unbounded penalty and gradient.} At the
multipliers each method reaches on the one-epoch probe, TraLO's per-cap
penalty \emph{(a)} flattens at its ceiling $\lambda(1{+}\rho)$ and the
gradient it applies \emph{(b)} stays capped and decays far from the cap,
while dual ascent's linear penalty and constant
$\lambda\!\approx\!53$ gradient grow without bound in $E$ and $\lambda$.
This is the shape of the \emph{objective}; whether the optimizer is
sensitive to it is the empirical question the body answers: the scale is
rescaled away, and the shape proves neutral by ablation and graft
(App.~\ref{app:shape}). Analytic, $K{=}10$.}
\label{fig:loss_shape}
\end{figure}

\begin{figure}[t]\centering
\includegraphics[width=\linewidth]{fig_mechanism}
\caption{\textbf{The escalating multiplier is discarded, not used: a
one-epoch-warmup probe} (DermMNIST $L50/G50$, RegNetY-400MF, seed~1).
\emph{(a)}~Dual ascent integrates the standing violation into a
multiplier that climbs to $\lambda{=}53$, while TraLO's ratchet freezes
at $\lambda\!\le\!0.19$. \emph{(b)}~The effect on the classifier is
none: every method takes a single norm-clipped constraint step per
epoch, so the two cross-entropy traces are numerically indistinguishable.
\emph{(c)}~The count does not fall until cross-entropy finishes
(train-accuracy gate, dotted), then reaches the cap for both. A
${\sim}280\times$ gap in $\lambda$ thus leaves training nearly identical:
with the constraint term dominant and the step norm-clipped, the update
is independent of $\lambda$. Adam's invariance to a constant rescaling of
the gradient \citep{kingma2014adam} points the same way, though we read
\emph{that} as an empirical observation rather than a theorem, since
$\lambda$ multiplies only the constraint term (so it shifts the
cross-entropy-versus-count weighting) and the Adam cancellation is exact
only as $\epsilon\to0$ and where the constraint term dominates.}
\label{fig:mechanism}
\end{figure}

% ----------------------------------------------------------------------------
\section{Complete Component Ablation}
\label{app:ablation}

This appendix is the source of the load-bearing finding of
Sec.~\ref{sec:carries}: of the four TraLO ingredients (saturating
penalty, ratcheted-and-frozen multiplier, Adam-state reset, undershoot
hinge), which two account for the constrained-class advantage.

A within-cell ablation isolates which ingredients carry the hard-regime
advantage: on the OctMNIST tight-cap cells where TraLO leads, we disable
one component at a time and measure the paired change in
constrained-class F1 (Table~\ref{tab:ablation_complete}; $n{=}24$ per
component). Two components are load-bearing. Removing the optimizer
reset at satisfaction costs $+0.079$ cc-F1, the single largest effect,
helping on all $24$ paired runs and hence in all six cells (cell-level
sign test $p\!=\!0.031$), because it breaks the post-satisfaction
descent momentum that would otherwise erode the constrained class.
Removing the undershoot hinge (the TraLO-bounded control, already in
the frozen grid) costs $+0.036$ cc-F1 (positive on all $24$ paired
runs; cell-level sign test $p\!=\!0.031$; $0.019$--$0.070$ across cells).
The other components are cc-F1-neutral on these already-satisfied
cells: the $\rho$ schedule and the lambda freeze move it by under $0.001$
(n.s.). A dedicated KL sweep on TissueMNIST (the $L30$ tight cap,
MobileNetV3, $\alpha_{\mathrm{KL}}\!\in\!\{0,0.1,0.3,1.0\}$) confirms the
anchor is off for good reason: $\alpha_{\mathrm{KL}}{=}0$ (the paper
recipe) gives the best constrained-class \emph{and} macro-F1, while
every nonzero anchor lowers cc-F1 (by $0.01$--$0.03$) and only adds
prediction edits. The KL term was a useful drift damper in an early
version of the method, before the optimizer reset and undershoot hinge;
once those two control post-satisfaction drift directly, the anchor is
redundant. The advantage rests on the reset and the hinge. One caveat
applies to all rows: the component importances are estimated inside the
selected win region (the tight-cap cells where TraLO leads), so they
quantify what carries that win, not what the components do elsewhere on
the grid. The reset, $\rho$, and freeze legs are a dedicated ablation
under the identical frozen recipe; the hinge leg is already in the main
grid.

{\color{blue}
\paragraph{The components are inert in a tie region.}
The caveat above --- that component importances are measured only inside the win
region --- invites the converse test: what do the two components do where TraLO does
\emph{not} win? We repeat the leave-one-out ablation on a tie cell (DermMNIST $L50$,
MobileNetV3, four seeds). There every component is inert: removing the optimizer
reset, the undershoot hinge, the $\rho$ schedule, or the lambda freeze moves cc-F1 by
at most $+0.006$ and macro-F1 by at most $0.003$ (largest: the reset, $+0.006$ cc-F1).
This is what the mechanism predicts --- the reset and hinge act only once the cap binds
and constrained-class recall is at stake --- and it tightens the paper's claim: the two
portable components carry the \emph{tight-cap} advantage specifically, not a diffuse
effect across the grid.}

\input{tables_rev/tab_ablation_complete}

\subsection{Grafting the two components onto the dual baselines}
\label{app:graft}

This subsection contains the graft evidence for the paper's central
mechanistic claim (Sec.~\ref{sec:carries}): the reset and hinge,
transplanted onto Fioretto-LDF and Hounie-RCL, close most of the
tight-cap gap. It is the converse of the within-TraLO ablation above.

The ablation above removes components from TraLO; the converse test adds
them to the baselines. We graft exactly the optimizer reset and the
undershoot hinge (each using the host's own multipliers and penalty)
onto Fioretto-LDF and Hounie-RCL and rerun the six tight-cap cells:
$2$ caps $\times$ $3$ backbones $\times$ $4$ seeds, $144$ runs in all,
including re-run controls for TraLO and both hosts under identical
warmup caches, the frozen recipe, and one GPU model. The re-run
controls reproduce the main-grid values to $\pm0.0013$ in all $18$
control cells, so the arms are compared on equal footing.
Table~\ref{tab:graft} reports mean cc-F1 per cell.

The graft lifts Fioretto-LDF in six of six cells ($+0.019$ to $+0.073$
cc-F1) and Hounie-RCL in five of six ($+0.035$ to $+0.103$; the one
exception, $L40$ on RegNet, drops by $0.028$), recovering a median
$86\%$ of TraLO's margin over the Fioretto host and $92\%$ over the
Hounie host (medians over the six cells, the loss cell included).
Against each grafted host separately TraLO stays best-or-tied ($8$
wins, $4$ ties, $0$ losses in the twelve paired comparisons under the
main-text win rule). Taken seed-by-seed against the stronger of the two
grafted hosts, though, its mean edge is only $+0.0015$ and it trails in
two of the six cells: once the recipe is grafted the three methods are
statistically interchangeable, with what remains of TraLO's lead
concentrated on the ViT-B/16 ($+0.015$/$+0.017$ over the grafted
Fioretto-LDF at the two caps). On macro-F1, TraLO versus the grafted
Fioretto-LDF is a tie in all six cells.

As an anti-windup control, we also run Fioretto-LDF with dual restarts:
all multipliers are reset to zero the moment the soft counts are
feasible, following \citet{gallego2022controlled}. Its deployed
predictions are \emph{identical} to plain Fioretto-LDF in every cell,
although every run fires at least one restart. The reason is
mechanistic: the deployed checkpoint is selected on the
constraint-excess axis before first feasibility is reached, and up to
that epoch the two arms are step-for-step identical, so machinery that
only alters post-feasibility dynamics cannot change the deployed model.
TraLO beats this arm in all six cells. The dual baselines' tight-cap
deficit is therefore not an artifact of missing anti-windup machinery,
and the linear-penalty comparison in the main grid is not a strawman in
that sense.

\subsection{The penalty shape is neutral}
\label{app:shape}

This subsection is the source of the paper's negative penalty-shape
finding (Sec.~\ref{sec:carries} and abstract): swapping
Eq.~\eqref{eq:loss}'s saturating penalty for a linear one, with the
reset, hinge, ratchet, and freeze held fixed, leaves constrained-class
predictions essentially unchanged.

The graft removes the question of whether TraLO's \emph{host} matters; a
third arm removes the question of whether its \emph{penalty} does. We
rerun TraLO on the same six cells with Eq.~(\ref{eq:loss})'s saturating
penalty replaced by a linear one, $\ell = E/K$, holding the ratchet, the
freeze, the optimizer reset and the undershoot hinge fixed, so the only
change is the shape the method is named for. The arm is genuinely
different, and by a wide margin: its logged constraint loss runs about
$3\times$ smaller throughout. Its results are not: cc-F1 moves by
$-0.001$ (the bounded shape wins $9$ of $24$ runs and loses $9$,
sign-test $p{=}1.0$; the six cell means split three positive and three
negative, none beyond $\pm0.006$) and median time-to-feasibility by two
constraint epochs. In $6$ of the $24$ runs the two arms land on
\emph{identical} cc-F1: changing the penalty shape changed no
prediction at all, which the fixed-direction argument below anticipates.

This is what the analysis of Sec.~\ref{sec:method} predicts, though the
reason is structural rather than a generic property of Adam. The
constraint penalty depends on the parameters only through the scalar
soft-count $S_c$, so its gradient is $\ell'(S_c)\,\nabla_\theta S_c$: a
scalar times a fixed direction. Multiplying the penalty by a positive
constant scales only that scalar, and behind Adam's coordinate-wise
normalization $g\!\to\!cg$ sends $m\!\to\!cm$, $\sqrt{v}\!\to\!c\sqrt{v}$,
leaving $\hat m/(\sqrt{\hat v}+\epsilon)$ unchanged but for the
$\epsilon$ term; the update along that direction is thus nearly
scale-free. The multiplier is such a scalar, so an escalating one
cannot by itself reach the weights: in the escalation probe of
App.~\ref{app:mech}, where $\lambda{=}53$ (nearly $280\times$ the
probe's $0.19$) drives $\|g\|{>}1$, it is the max-norm rail that caps
the step, and the count falls no faster than at $\lambda\!\le\!0.19$.
The penalty \emph{shape} and the weight $\rho$ reshape $\ell'(S_c)$
rather than rescale it, so they are not covered by the constant-$c$
argument; we establish their neutrality empirically instead, by graft
(shape) and by the ablation rows of Table~\ref{tab:ablation_complete}
($\rho$ schedule, freeze). What no rescaling touches is Adam's own
accumulated state, and the hinge, which reverses the direction to
$-\nabla_\theta S_c$ while the count undershoots: these are the
components the ablation ranks first and the graft transfers.

We report this as a negative result about our own formulation.
Eq.~(\ref{eq:loss}) earns its place by keeping the multiplier small and
readable, and by making the two-phase argument of Sec.~\ref{sec:method}
available at all; it does not earn the constrained-class gains, which
belong to the reset and the hinge.

\subsection{TraLO's own hyperparameters are not doing the work}
\label{app:tralo-sweep}

This subsection answers a natural robustness question: are TraLO's own
tunable knobs (the hinge weight $\beta$ and the ratchet step
$\lambda_{\text{step}}$) what deliver the OctMNIST tight-cap advantage
of Sec.~\ref{sec:oct}? They are not.

The dual baselines' step size is swept an order of magnitude to confirm
their tuning is not what wins or loses the comparison
(App.~\ref{app:mech}); fairness asks the same of TraLO's own
continuous knobs, the more so in a transductive setting with no
held-out split. On the six OctMNIST tight cells we sweep the hinge
weight $\beta\in\{0.25,0.5,0.75,1.0\}$ and the ratchet step
$\lambda_{\text{step}}\in\{0.001,0.002,0.004\}$ around the frozen
recipe ($\beta{=}0.5$, $\lambda_{\text{step}}{=}0.002$), one knob at a
time, $24$ runs per arm, with everything else, warmup cache included,
held fixed ($120$ runs).

\textbf{The ratchet step is neutral.} Moving $\lambda_{\text{step}}$
over a factor of four shifts mean cc-F1 by at most $0.0012$ and the
median time-to-feasibility by under an epoch. This is the
scalar-rescaling argument of Sec.~\ref{sec:method} made good:
$\lambda_{\text{step}}$ sets only how fast the multiplier climbs, and
the multiplier is a positive scalar on the fixed soft-count direction,
which Adam's normalization and the clip rail absorb.

\textbf{The hinge weight matters, but the advantage is not a
knife-edge at $\beta{=}0.5$.} As the one continuous knob on a
load-bearing component, $\beta$ does move cc-F1, but by at most $0.003$
in the six-cell mean across the whole $\{0.25,\dots,1.0\}$ range,
smallest at the weakest hinge $\beta{=}0.25$, exactly where less hinge
restores less constrained-class recall. More to the point, TraLO
clears the best trained baseline in all $30$ arm-by-cell comparisons,
with margins from $+0.014$ to $+0.088$: the tight-cap advantage
survives every setting of the two knobs, not only the values we fixed
a priori.

\subsection{The convergence ordering is not an initialization artifact}
\label{app:init}

This subsection rules out that TraLO's faster feasibility over the dual
baselines (Sec.~\ref{sec:conv}) is an artifact of its non-zero multiplier
initialization, by rerunning each dual baseline from TraLO's own start
value.

TraLO starts its multiplier at $\lambda{=}0.05$ while the dual
baselines start at $\lambda{=}0$ and integrate upward, so one could
read TraLO's faster feasibility (Sec.~\ref{sec:conv}) as a head start
rather than a property of the method. We test this on the six OctMNIST
tight cells by rerunning each dual baseline from TraLO's own
$\lambda{=}0.05$ start, changing nothing else ($24$ runs each). The
nonzero start leaves Fioretto-LDF's median time-to-feasibility
unchanged ($28$ constraint epochs, taken over the runs that reach
feasibility) and it still fails to reach the count natively on $6$ of
$24$ runs; it narrows Hounie-RCL's median from $40$ to $31$
(Hounie-RCL reaches feasibility on every run, so its median is over
all $24$). Both still trail TraLO's $25$. So the head start explains
part of Hounie's gap and little of Fioretto's, and closes neither: on
these cells the ratchet-and-freeze schedule reaches and holds the
count faster than either a zero- or a nonzero-start dual ascent. This
control is specific to the OctMNIST tight cells; the cross-dataset
census of Sec.~\ref{sec:conv} uses each method as published.

\input{tables/tab_graft}

% ----------------------------------------------------------------------------
\section{Native Satisfaction: a Deployment Property, not a Headline}
\label{app:deploy}

This appendix documents the deployment properties (native satisfaction
and prediction flips) that back the macro-F1 advantage over post-hoc
clipping of Sec.~\ref{sec:deploy}. It is deliberately separated from the
main quality claims because, on its own, native satisfaction does not
discriminate methods: every deployable system meets a hard count one
way or another.

\input{tables/tab_deploy}

We separate \emph{native satisfaction} (meeting the count with no
post-hoc edit) from the quality results in the main paper because, on
its own, it does not discriminate methods: every deployable system meets
a hard count one way or another, and post-hoc clipping reaches
satisfaction $1.00$ by construction, simply by editing predictions. We
report it only to document the mechanism behind TraLO's quality
advantage over clipping (Sec.~\ref{sec:deploy}): TraLO reaches the
count during training and so never invokes the editing step that costs
clipping its accuracy. Table~\ref{tab:deploy} gives the deployment costs
at the tight $L30$ cap on MobileNetV3: macro-F1, accuracy, and the
number of predictions each method must overwrite (Flips).
Figure~\ref{fig:deployment} shows native satisfaction per method over
the full grid. TraLO and Hounie-RCL meet the count in every run;
Fioretto-LDF and the hinge-free TraLO-bounded do most of the time; the
post-hoc clippers meet it natively only at loose caps. A caveat on
\emph{flips}: at near-trivial loose caps (e.g.\ OctMNIST $L80/L90$)
TraLO can record more flips than a clipper that barely has to act, so
the robust statement is ``satisfies natively while preserving
macro-F1,'' never a raw flip count.

The macro-F1 advantage over clipping is not specific to MobileNetV3 or
to the single $L30$ cap of Table~\ref{tab:deploy}.
Table~\ref{tab:deploybb} averages the paired gap over the symmetric caps
for each dataset--backbone cell, adding MobileNetV2 to the three
main-grid backbones: the mean gain is positive in every one of the
twelve cells, largest on TissueMNIST, and it persists under asymmetric
caps (App.~\ref{app:regime}).

\input{tables/tab_deploy_backbone}

\begin{figure}[t]\centering
\includegraphics[width=\linewidth]{fig_deployment}
\caption{\textbf{Methods trained under the constraint satisfy the count
natively; post-hoc clippers do not.} Mean native satisfaction per
method and backbone over the paper\_final grid ($N{=}108$ runs per
method$\times$backbone: $3$ datasets $\times\,9$ caps $\times\,4$
seeds); the dots are the three per-dataset means. TraLO and Hounie-RCL
meet the count in every run (satisfaction $1.00$); Fioretto-LDF and
TraLO-bounded reach it most of the time ($\sim\!0.85$--$0.91$); the
post-hoc clippers satisfy natively only at loose caps
($\sim\!0.14$--$0.15$) and otherwise reach the cap solely by editing
predictions, the source of their accuracy tax (Table~\ref{tab:deploy}).}
\label{fig:deployment}
\end{figure}

% ----------------------------------------------------------------------------
\section{Per-Cell Results Across the Full Grid}
\label{app:regime}

This appendix is the per-cell substrate of the regime map claim
(Sec.~\ref{sec:regime}): every dataset$\times$backbone$\times$cap cell in
the paper\_final grid is listed with its paired gaps and win/tie/loss
classification, so that any regime-level assertion in the body can be
verified directly against the individual cells behind it.

Tables~\ref{tab:gran_tissue}--\ref{tab:gran_asym} give the fully granular
breakdown behind the regime summary of Sec.~\ref{sec:regime}: one row
per (dataset, backbone, cap), averaged over the four seeds \emph{only},
with no pooling across backbones or caps. Each row pairs TraLO against
the best constraint-trained baseline on constrained-class F1 (per-seed
best of Fioretto-LDF/Hounie-RCL) and against the best post-hoc clipper
on macro-F1, both paired by seed.

Two patterns read straight off the cells. The constrained-class
advantage is \emph{consistent} (all three backbones, every seed) only
at the OctMNIST tight caps ($L30$/$L40$), where it is largest on
ViT-B/16; a handful of isolated single-backbone cells elsewhere also
clear the $+0.005$ bar, but no other regime wins across backbones, and
none is a loss beyond seed noise. The macro-F1 margin over clipping is
positive in $77$ of the $81$ symmetric cells; the four exceptions are
the very tightest $L10$/$L20$ caps, where the cap nearly zeroes out the
constrained class and the gap is within $\pm0.02$. The asymmetric caps
(Table~\ref{tab:gran_asym}) hold the \emph{preliminary}
constrained-class win, on DermMNIST where the global cap is tighter
than the group cap. That per-cell table is MobileNetV3, the only
backbone with the full asymmetric grid; a cross-backbone check on the
four shared $G{<}L$ cap pairs (Table~\ref{tab:asym_xbackbone}) shows
the constrained-class advantage reproduces on RegNet ($+0.9$pp, $75\%$
seed-winrate) and ties on the ViT ($-0.0$pp), while the macro-F1
advantage over clipping holds on all three backbones. We report the
asymmetric result as preliminary because only MobileNetV3 has been run
on the complete asymmetric grid, not because a single backbone shows it.

Table~\ref{tab:bbgen} summarizes the constrained-class picture across
backbones, adding MobileNetV2 to the three main-grid backbones and
averaging the paired gap over the symmetric caps: on TissueMNIST and
DermMNIST no backbone shows a systematic advantage (means within
$\pm0.003$), while OctMNIST is positive on all four. The honest tie
and the OctMNIST advantage are each backbone-general, not artifacts of
one architecture. Table~\ref{tab:oct_backbone} isolates the OctMNIST
tight caps ($L30$/$L40$) behind that positive row: the paired delta
over the best trained dual is positive at every backbone$\times$cap
cell on cc-F1 (all four seeds) and improves macro-F1 at five of the
six cells, peaking on the ViT-B/16. This is the per-backbone breakdown
Sec.~\ref{sec:oct} cites next to its consolidated grid
(Table~\ref{tab:ccf1}).

\input{tables_rev/tab_backbone_generality}

\input{tables/tab_oct_backbone}

\begin{table}[t]\centering\small
\setlength{\tabcolsep}{5pt}
\caption{\textbf{The DermMNIST global-tighter ($G{<}L$) win reproduces
across backbones.} Cross-backbone check on the four shared $G{<}L$
cap pairs ($L50/G20$, $L50/G30$, $L70/G50$, $L80/G70$; mean over
seeds $1$--$4$, $n{=}16$ paired seed-cells). $\Delta$cc-F1 is TraLO
minus the best constraint-trained baseline; $\Delta$macro-F1 is TraLO
minus the best post-hoc clipper; both paired by seed, with \emph{win}
the seed-cell fraction favoring TraLO. The constrained-class advantage
holds on MobileNetV3 and RegNet and ties on the ViT; the macro-F1
advantage over clipping holds on all three.}
\label{tab:asym_xbackbone}
\setlength{\tabcolsep}{4pt}\small
\begin{tabular}{l rc rc}
\toprule
 & \multicolumn{2}{c}{$\Delta$cc-F1 vs.\ trained} & \multicolumn{2}{c}{$\Delta$macro-F1 vs.\ clipper} \\
\cmidrule(lr){2-3}\cmidrule(lr){4-5}
Backbone & $\Delta$ & win & $\Delta$ & win \\
\midrule
MobileNetV3    & $+0.012$ & $81\%$  & $+0.015$ & $77\%$ \\
RegNetY-400MF  & $+0.009$ & $75\%$  & $+0.002$ & $50\%$ \\
ViT-B/16       & $-0.000$ & $38\%$  & $+0.032$ & $100\%$ \\
\bottomrule
\end{tabular}
\end{table}

\input{tables/tab_granular_tissue}
\input{tables/tab_granular_derm}
\input{tables/tab_granular_oct}
\input{tables/tab_granular_asym}

% ----------------------------------------------------------------------------
\section{Datasets and Group Construction}
\label{app:groups}

This appendix documents the class distributions and the group
construction that back the constrained-class and per-group cap
definitions of Sec.~\ref{sec:setup}. It also elaborates on the
ceiling-versus-floor caveat that motivates the fairness scoping in the
Broader Impact statement.

% [Revision] This figure was moved here from Section 1; its caption is marked
% blue so the relocation is identifiable alongside the other revision marks.
\begin{figure}[H]\centering
\includegraphics[width=0.5\columnwidth]{fig_datasets}
\captionsetup{labelfont={color=blue}}
\caption{\color{blue}\textbf{DermMNIST lesions, benign to malignant} (green frame benign,
red malignant, blue the \emph{constrained} class), shown here as one
representative example of the three MedMNIST tasks used in the paper.
The task caps how many test images may be labeled melanoma, the minority
high-stakes class ($11.1\%$ of the test set); the constraint-trained
methods studied here meet that cap \emph{during training} rather than
clipping afterward.}
\label{fig:datasets}
\end{figure}

The constrained class is a genuine minority on two of the three
datasets and not on the third: GE is $7.1\%$ of the TissueMNIST test
set ($171/2400$) and melanoma is $11.1\%$ of DermMNIST ($223/2003$),
while OctMNIST's test split is class-balanced by construction ($250$
per class), making drusen $25\%$. We keep OctMNIST because it is where
the cap binds hardest, not because it matches the screening story: at
$L30$ the cap admits $75$ of $250$ true drusen, so a structural
majority of them cannot be flagged whatever the model does. Readers
should not take the OctMNIST result as evidence about rare-class
screening, and a clinical deployment would more plausibly cap the
urgent classes (CNV, DME) than drusen.

Local caps are only over real groups on DermMNIST. There we pool
HAM10000's \texttt{localization} field into three anatomical groups:
torso (back, trunk, abdomen, chest, genital, unknown), extremity
(upper/lower extremity, foot, hand, acral), and head/neck (face, neck,
scalp, ear), giving $1030/738/235$ test images carrying $98/85/40$
melanomas. The groups are deliberately unequal in both size and
prevalence, which is what makes the per-group caps bind differently.

TissueMNIST and OctMNIST carry no group metadata, so their local caps
use a fixed synthetic binary split drawn once from a seeded generator
and stored with the data. These groups are arbitrary by construction.
They exercise the mechanism, several simultaneous caps that must hold
at once alongside the global cap, and nothing else: no fairness
reading of the TissueMNIST or OctMNIST local caps is available, and we
do not offer one. Even on DermMNIST, where the groups are real, a
local cap is a per-group \emph{ceiling} on flagging melanoma rather
than a floor on detecting it, so satisfying it is not evidence of
equitable treatment. A deployment that set these caps from estimated
prevalence would inherit whatever under-diagnosis those estimates
already encode. We report the mechanism and leave the allocation
question, which is a question about who sets $K_g$ and on what
evidence, outside our claims.

\end{document}
